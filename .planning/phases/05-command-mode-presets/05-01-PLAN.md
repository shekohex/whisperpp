---
phase: 05-command-mode-presets
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/data/TransformPresets.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
  - android/app/src/main/res/values/strings.xml
  - android/app/src/test/java/com/github/shekohex/whisperpp/data/TransformPresetsTest.kt
autonomous: true
requirements:
  - ENH-05

must_haves:
  truths:
    - "A shared preset library exists with at least 3 presets: cleanup, shorten, tone rewrite"
    - "Enhancement mode and command mode each have an independently persisted default preset selection"
    - "User can change enhancement default preset and command default preset from Settings"
  artifacts:
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/data/TransformPresets.kt"
      provides: "Shared preset library + lookup by ID"
      contains: "cleanup"
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt"
      provides: "DataStore preference keys for preset defaults"
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt"
      provides: "Settings UI to choose per-mode preset defaults"
  key_links:
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt"
      to: "android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt"
      via: "Flow read + DataStore edit write"
      pattern: "dataStore\\.edit|SettingsRepository"
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/data/TransformPresets.kt"
      to: "android/app/src/main/res/values/strings.xml"
      via: "@StringRes title/description"
      pattern: "R\\.string\\."
---

<objective>
Add a shared transform preset library (>= 3 presets) and persisted per-mode defaults (enhancement vs command), with minimal settings UI to select the defaults.

Purpose: Satisfy ENH-05 foundation so both enhancement and command mode can reference the same preset IDs.
Output: Transform preset model/list, DataStore keys + repository accessors, and settings UI for defaults.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-command-mode-presets/05-CONTEXT.md
@.planning/phases/05-command-mode-presets/05-RESEARCH.md

@android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared TransformPresets library + unit coverage</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/data/TransformPresets.kt
android/app/src/main/res/values/strings.xml
android/app/src/test/java/com/github/shekohex/whisperpp/data/TransformPresetsTest.kt
  </files>
  <action>
- Create `TransformPresets.kt` in `com.github.shekohex.whisperpp.data`:
  - Define a stable preset ID type (use `typealias TransformPresetId = String` or a small value class).
  - Define a `TransformPreset` model with:
    - `id`
    - `@StringRes titleRes`
    - `@StringRes descriptionRes` (short 1-liner for UI)
    - `promptInstruction: String` (LLM-facing instruction fragment; not shown to user)
  - Export:
    - `val TRANSFORM_PRESETS: List<TransformPreset>` (shared list)
    - `fun presetById(id: TransformPresetId?): TransformPreset?`
    - Constants for the 3 locked starter presets: `cleanup`, `shorten`, `tone_rewrite`.
  - Keep promptInstruction intent simple and deterministic (no dynamic content, no Markdown).

- Update `strings.xml` with title/description resources for each preset.

- Add `TransformPresetsTest.kt`:
  - Asserts at least 3 presets exist.
  - Asserts required IDs exist: cleanup, shorten, tone_rewrite.
  - Asserts IDs are unique and `presetById` returns null for unknown IDs.
  </action>
  <verify>
    <automated>./android/gradlew -p android testDebugUnitTest</automated>
  </verify>
  <done>
- Transform preset library is present, includes the 3 required presets, and is covered by a unit test.
  </done>
</task>

<task type="auto">
  <name>Task 2: Persist per-mode default preset IDs (enhancement + command)</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt
android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt
  </files>
  <action>
- Add two Preferences DataStore keys in `MainActivity.kt` (follow existing key style):
  - `ENHANCEMENT_PRESET_ID` (string)
  - `COMMAND_PRESET_ID` (string)

- In `SettingsRepository` add Flow + setters for these defaults:
  - `val enhancementPresetId: Flow<String?>`
  - `val commandPresetId: Flow<String?>`
  - `suspend fun setEnhancementPresetId(id: String?)`
  - `suspend fun setCommandPresetId(id: String?)`

- Defaults:
  - If unset, treat enhancement default as `cleanup`.
  - If unset, treat command default as `tone_rewrite` (or `cleanup` if you prefer; must be independent keys either way).
  - Do not add any migration; new keys are additive.
  </action>
  <verify>
    <automated>./android/gradlew -p android testDebugUnitTest</automated>
  </verify>
  <done>
- Enhancement and command preset defaults are persisted independently and exposed via SettingsRepository.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add settings UI for preset defaults (enhancement + command)</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
android/app/src/main/res/values/strings.xml
  </files>
  <action>
- Add a settings destination to configure preset defaults (exact route name discretion; keep consistent with existing navigation patterns).
  - UI must show:
    - Enhancement default preset selector
    - Command default preset selector
  - Each selector can be implemented as a simple list radio picker or two-step picker.
  - Options come from `TRANSFORM_PRESETS` list.
  - Persist selections using the new SettingsRepository setters.

- Ensure strings are resource-based (screen title, labels, helper text).
  </action>
  <verify>
    <automated>./android/gradlew -p android :app:assembleDebug testDebugUnitTest</automated>
  </verify>
  <done>
- Settings exposes both preset defaults, updates persist, and the app compiles.
  </done>
</task>

</tasks>

<verification>
- `./android/gradlew -p android :app:assembleDebug testDebugUnitTest` passes.
</verification>

<success_criteria>
- ENH-05 foundation is present: shared library (>=3 presets) + per-mode default IDs persisted + settings UI to change them.
</success_criteria>

<output>
After completion, create `.planning/phases/05-command-mode-presets/05-01-SUMMARY.md`
</output>
