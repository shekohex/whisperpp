---
phase: 02-providers-models
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreen.kt
  - android/app/src/test/java/com/github/shekohex/whisperpp/data/ProviderSelectionsMigrationTest.kt
autonomous: true
requirements:
  - PROV-03
  - PROV-04

must_haves:
  truths:
    - "User explicitly selects active STT provider+model and active text provider+model"
    - "Command mode inherits text selection unless an advanced override is set"
    - "Invalid/missing selections surface an immediate setup-needed banner with CTA to selectors"
  artifacts:
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt"
      provides: "DataStore keys for active provider/model selections"
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt"
      provides: "Selectors UI (two-step pickers) + setup-needed banner"
  key_links:
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt"
      to: "androidx.datastore.preferences.core.DataStore"
      via: "persist active selections"
      pattern: "edit \{.*(stt|text).*\}"
---

<objective>
Implement Phase 2 selection UX: explicit STT and text provider/model selections (two-step compatible pickers), shared text with optional advanced command override, clear-to-none support, and immediate setup-needed banner when selections are missing/invalid.

Purpose: Deliver PROV-03 and PROV-04 selection behavior and ensure the UI reflects Phase 2 locked decisions.
Output: New selection preference keys + selectors UI + setup-needed banner + Privacy & Safety screens updated to use selections.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-providers-models/02-CONTEXT.md
@.planning/phases/02-providers-models/02-RESEARCH.md

@android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreen.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DataStore keys for STT/text selections + one-time migration from legacy keys</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt
android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt
android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
android/app/src/test/java/com/github/shekohex/whisperpp/data/ProviderSelectionsMigrationTest.kt
  </files>
  <action>
- In `MainActivity.kt`, add new preference keys:
  - `ACTIVE_STT_PROVIDER_ID`, `ACTIVE_STT_MODEL_ID`
  - `ACTIVE_TEXT_PROVIDER_ID`, `ACTIVE_TEXT_MODEL_ID`
  - `COMMAND_TEXT_PROVIDER_ID`, `COMMAND_TEXT_MODEL_ID` (optional override)

- In `SettingsRepository.kt`, add a one-time migration flag (boolean) and a migration function that:
 - In `SettingsRepository.kt`, add a one-time migration flag (boolean) and a migration function that:
  - If the new keys are unset:
    - Copy `SPEECH_TO_TEXT_BACKEND` → `ACTIVE_STT_PROVIDER_ID`.
    - Copy `MODEL` (if present) → `ACTIVE_STT_MODEL_ID` (otherwise leave blank).
    - Copy `SMART_FIX_BACKEND` → `ACTIVE_TEXT_PROVIDER_ID`.
    - Copy `SMART_FIX_MODEL` → `ACTIVE_TEXT_MODEL_ID`.
    - Leave command override keys blank (inherit).
  - Mark migration complete.

- Make migration logic unit-testable:
  - Implement a pure helper used by the migration, e.g. `internal fun migrateLegacySelectionsToV2(prefs: MutablePreferences)`.
  - Add unit tests in `ProviderSelectionsMigrationTest.kt` verifying:
    - When new keys are empty, legacy keys are copied as specified.
    - When new keys already exist, migration is a no-op.

- In `SettingsNavigation` (SettingsScreen.kt), run this selection migration before rendering NavHost.
  </action>
  <verify>
 JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew -p android testDebugUnitTest :app:assembleDebug
  </verify>
  <done>
- New selection keys exist.
- Existing installs retain their current provider choices via migration.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build selectors UI (two-step pickers) with compatibility filtering by model kind</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
  </files>
  <action>
- Add a dedicated selectors UI reachable from settings (may be a new route or the top of the existing Providers screen), implementing locked decisions:
  - Explicit selection required (no auto-select first provider/model).
  - Two-step picker UX:
    - Step 1: choose provider
    - Step 2: choose model filtered to that provider
  - Compatibility filtering is **model-kind only**:
    - STT picker shows models where kind is STT or MULTIMODAL.
    - Text picker shows models where kind is TEXT or MULTIMODAL.
    - Do not hard-block by provider type.
  - Clear-to-none:
    - Include an explicit "None" option that clears provider+model for that selection.
  - Shared text + optional advanced command override:
    - Default command uses the shared text selection.
    - Advanced section allows setting a command override provider+model; when unset, it inherits shared text selection changes.
  </action>
  <verify>
JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew -p android :app:assembleDebug
  </verify>
  <done>
- Users can select STT provider+model and text provider+model.
- Command override exists as an optional advanced override and inherits by default.
  </done>
</task>

<task type="auto">
  <name>Task 3: Setup-needed banner + invalid-selection handling + update Privacy & Safety to reflect selections</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreen.kt
  </files>
  <action>
- Add selection validation logic:
  - If selected providerId does not exist, or selected modelId does not exist under that provider, treat selection as invalid.
  - Do not auto-fallback to another provider/model.
  - Clear invalid selections to none and surface setup-needed state.

- Make selection validation unit-testable:
  - Implement a pure helper (in `SettingsRepository.kt` or a small new file in `data/`) used by the UI to compute validity and which keys to clear.
  - Extend `ProviderSelectionsMigrationTest.kt` with cases:
    - Selected provider missing → provider+model cleared
    - Selected model missing under provider → model cleared (provider retained)
    - Command override invalid → override cleared; inherited shared text remains

- Setup-needed banner:
  - Show an immediate persistent banner in settings when either:
    - STT selection missing/invalid, or
    - Text selection missing/invalid.
  - Banner CTA must navigate directly to the selectors UI.
  - Dictation availability is not blocked by missing text selection; only text-dependent features are blocked (handled in runtime wiring plan).

- Update `PrivacySafetyScreen` disclosure generation to use the active selections (STT + Text + Command override with inheritance) instead of the legacy keys.

- Deletion requires reassignment before delete completes (locked decision):
  - Provider delete (providers list):
    - If a provider is currently selected by any of:
      - `ACTIVE_STT_PROVIDER_ID`
      - `ACTIVE_TEXT_PROVIDER_ID`
      - `COMMAND_TEXT_PROVIDER_ID`
      then block deletion and present a blocking dialog that:
        - states which selection(s) reference it (STT/Text/Command override)
        - provides a CTA to navigate directly to the selectors UI
        - prevents completing deletion until user reassigns (selects a replacement provider/model) or explicitly clears that selection to None.
  - Model delete (provider edit screen):
    - If a model is currently selected for the provider being edited (matches providerId + modelId for STT/Text/Command override), then block deletion and require reassignment/clear first, with the same selectors CTA.

- Update `PrivacySafetyScreen` disclosure generation to use the active selections (STT + Text + Command override with inheritance) instead of the legacy keys.
  </action>
  <verify>
JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew -p android :app:assembleDebug
  </verify>
  <done>
- Missing/invalid selections are surfaced immediately with a banner and direct navigation to fix.
- Privacy disclosures reflect the selected providers/models.
  </done>
</task>

</tasks>

<verification>
- Build: `JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew -p android :app:assembleDebug`
</verification>

<success_criteria>
- PROV-03 satisfied in settings: explicit STT provider+model selection with compatible filtering.
- PROV-04 satisfied in settings: explicit shared text provider+model selection with optional advanced command override.
- Setup-needed state is immediate and navigates directly to selectors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-providers-models/02-03-SUMMARY.md`
</output>
