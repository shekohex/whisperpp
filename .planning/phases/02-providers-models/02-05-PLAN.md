---
phase: 02-providers-models
plan: 05
type: execute
wave: 5
depends_on:
  - "02-04"
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/SmartFixer.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/WhisperTranscriber.kt
autonomous: true
requirements:
  - PROV-01
  - PROV-05
  - PROV-06

must_haves:
  truths:
    - "Provider details include a manual Test action after save"
    - "Tests are capability-specific (STT/text) and can be forced even if likely incompatible"
    - "Test results show full raw provider response (UI-only) without leaking API keys"
    - "User can optionally import/fetch a model list into a provider"
  artifacts:
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt"
      provides: "Provider Test UI + model import UX"
  key_links:
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt"
      to: "android/app/src/main/java/com/github/shekohex/whisperpp/SmartFixer.kt"
      via: "reuse base-URL endpoint building for tests"
      pattern: "chat/completions|generateContent"
---

<objective>
Add provider-level manual testing and optional model import: capability-specific STT/text tests (with test-anyway option) and raw provider response display in UI, while maintaining privacy constraints (no API key leakage).

Purpose: Complete Phase 2 UX for diagnosing provider configuration and support optional model discovery/import.
Output: Provider test actions + raw response UI + optional model import into provider model list.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-providers-models/02-CONTEXT.md
@.planning/phases/02-providers-models/02-RESEARCH.md

@android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/WhisperTranscriber.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/SmartFixer.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add provider Test actions (Text + STT) with raw response UI</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
  </files>
  <action>
- In provider details/edit screen (only after provider exists / has an id), add a non-blocking "Test" section.

- Endpoint construction for tests (make explicit):
  - Reuse the same base-URL routing rules introduced in 02-04:
    - OpenAI-compatible text: `{baseUrl}/chat/completions`
    - OpenAI-compatible STT: `{baseUrl}/audio/transcriptions`
    - Gemini-compatible text: `{baseUrl}/models/{model}:generateContent`
  - Implement a single helper in `SettingsScreen.kt` (or a small shared helper file) that builds these URLs using OkHttp `HttpUrl` (safe join, preserves ports).
  - Do not duplicate URL-building logic in multiple composables.

- Capability-specific tests:
  - Text test:
    - Uses a chosen text-capable model (kind TEXT/MULTIMODAL) with a minimal prompt like `Reply with OK`.
    - Executes the same HTTP path as Smart Fix (OpenAI chat completions or Gemini generateContent depending on provider.type).
  - STT test:
    - Uses a chosen STT-capable model (kind STT/MULTIMODAL).
    - Let user pick an audio file via document picker (audio/*), then send it to the OpenAI-compatible transcription endpoint derived from base URL.

- "Test anyway" support:
  - If no compatible models exist, allow an advanced mode where user can type a model id and run the test anyway.

- Result display:
  - Show HTTP status and full raw response body in a scrollable dialog/sheet.
  - Never show API keys in UI.
  - Do not log response bodies.
  - Before showing raw response, redact any accidental secret echoes (e.g., `key=...`, `Authorization: Bearer ...`, `x-goog-api-key: ...`) via simple string redaction.
  </action>
  <verify>
JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew -p android :app:assembleDebug
  </verify>
  <done>
- Provider screen includes Text/STT tests.
- Test results show raw provider response in UI.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add optional model fetch/import per provider (non-blocking)</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt
  </files>
  <action>
- Add an optional "Import models" action in provider edit:
  - OpenAI-compatible: `GET {baseUrl}/models` (auth per authMode).
  - Gemini-compatible: `GET {baseUrl}/models` (auth per authMode) and surface raw provider response on failure (Gemini model listing varies).
  - Parse returned model IDs and add to the provider model list (dedupe by id).
  - Default imported model metadata:
    - OpenAI: kind=TEXT
    - Gemini: kind=MULTIMODAL
    - streamingPartialsSupported=false

- Persist imported models by saving providers list via `SettingsRepository.saveProviders`.
- Failures should show the raw response body in the same test-result UI style, without logging secrets.
  - Make explicit: model import uses the same URL-building helper as provider tests (base URL + relative path join).
  </action>
  <verify>
JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew -p android :app:assembleDebug
  </verify>
  <done>
- Users can optionally fetch/import models into a provider; failures are actionable with raw response visibility.
  </done>
</task>

<task type="auto">
  <name>Task 3: Ensure verbose logging remains secret-safe when Gemini auth uses query params</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/SmartFixer.kt
android/app/src/main/java/com/github/shekohex/whisperpp/WhisperTranscriber.kt
  </files>
  <action>
- Prefer sending Gemini auth via `x-goog-api-key` header (consistent with current `SmartFixer`), but if query-param auth is required, ensure verbose network logs redact `key=...` from the request line before logging (Phase 1 requires redacted verbose logs).
- Ensure OpenAI Authorization and Gemini key headers remain redacted as in Phase 1.
  </action>
  <verify>
JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew -p android :app:assembleDebug
  </verify>
  <done>
- Enabling verbose network logs does not reveal API keys (including query-param based keys).
  </done>
</task>

</tasks>

<verification>
- Build: `JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew -p android :app:assembleDebug`
</verification>

<success_criteria>
- Provider test UX exists and shows raw provider responses in UI (per locked decision) without leaking secrets.
- Optional model import exists.
- OpenAI and Gemini compatibility can be diagnosed end-to-end from settings.
</success_criteria>

<output>
After completion, create `.planning/phases/02-providers-models/02-05-SUMMARY.md`
</output>
