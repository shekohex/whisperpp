---
phase: 02-providers-models
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/data/ProviderModels.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/data/Presets.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatter.kt
  - android/app/src/test/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatterTest.kt
  - android/app/src/test/java/com/github/shekohex/whisperpp/data/ProviderSchemaV2MigrationTest.kt
autonomous: true
requirements:
  - PROV-01
  - PROV-02

must_haves:
  truths:
    - "Existing installs keep providers and stored API keys still apply after schema upgrade (no re-entry required)"
    - "Providers are configured by base URL + auth mode (not per-operation endpoints)"
    - "Provider models include kind (stt/text/multimodal) and streaming-partials capability"
  artifacts:
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/data/ProviderModels.kt"
      provides: "Provider schema v2 (auth mode, model kind, streaming flag)"
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt"
      provides: "One-time provider schema migration (endpoint->base URL, default new fields)"
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatter.kt"
      provides: "Disclosure paths derived from base URL + operation"
    - path: "android/app/src/test/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatterTest.kt"
      provides: "Unit coverage for disclosure URL/path derivation"
    - path: "android/app/src/test/java/com/github/shekohex/whisperpp/data/ProviderSchemaV2MigrationTest.kt"
      provides: "Unit coverage for provider schema v2 migration rewrites"
  key_links:
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt"
      to: "android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt"
      via: "run schema migration before NavHost renders"
      pattern: "migrateProviderSchemaV2"
---

<objective>
Introduce Provider/Model schema v2: base URL + auth mode, model kind + streaming partials support; migrate existing saved providers; keep privacy disclosures accurate with derived operation paths.

Purpose: Establish the data model needed for PROV-01/02 and enable downstream selection + routing without breaking existing installs.
Output: Updated provider/model types + presets + schema migration + disclosure derivation tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-providers-models/02-CONTEXT.md
@.planning/phases/02-providers-models/02-RESEARCH.md

@android/app/src/main/java/com/github/shekohex/whisperpp/data/ProviderModels.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatter.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend provider/model types for base URL + auth mode + model kind/capabilities</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/data/ProviderModels.kt
android/app/src/main/java/com/github/shekohex/whisperpp/data/Presets.kt
  </files>
  <action>
- In `ProviderModels.kt`:
  - Add `enum class ModelKind { STT, TEXT, MULTIMODAL }`.
  - Add `enum class ProviderAuthMode { API_KEY, NO_AUTH }`.
  - Update `ModelConfig` to include:
    - `kind: ModelKind` (default: TEXT)
    - `streamingPartialsSupported: Boolean` (default: false)
    - Keep existing `isThinking` (default: false).
  - Update `ServiceProvider` to include:
    - `authMode: ProviderAuthMode` (default: API_KEY)
    - Keep `endpoint: String` field name for JSON compatibility, but treat its semantics as **base URL** going forward (no per-operation endpoints).
    - Keep `@Transient apiKey` runtime-only.

- In `Presets.kt`, update presets to match the new semantics:
  - OpenAI preset(s) should use base URL `https://api.openai.com/v1`.
  - Gemini preset should use base URL `https://generativelanguage.googleapis.com/v1beta`.
  - Assign model kinds:
    - Whisper model(s): `STT`.
    - Chat/LLM models: `TEXT`.
    - Gemini models: `MULTIMODAL`.
  - Ensure STT-capable models default `streamingPartialsSupported=false` per Phase 2 context.
  </action>
  <verify>
JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew -p android :app:assembleDebug
  </verify>
  <done>
- Code compiles with new fields and updated presets.
- Providers can be represented with base URL + auth mode, and models carry kind + streaming capability metadata.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add one-time provider schema migration (endpoint → base URL, default new fields)</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt
android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
android/app/src/test/java/com/github/shekohex/whisperpp/data/ProviderSchemaV2MigrationTest.kt
  </files>
  <action>
- In `SettingsRepository.kt`:
  - Add a migration flag preference key (boolean), e.g. `PROVIDER_SCHEMA_V2_MIGRATION_DONE`.
  - Implement `suspend fun migrateProviderSchemaV2IfNeeded(context: Context)` that:
    - Runs after `migrateProviderApiKeysIfNeeded(context)`.
    - Loads raw `PROVIDERS_JSON` and parses it as `JsonElement` (same pattern as API-key migration).
    - Implement a pure helper (unit-testable) to migrate the JSON tree, e.g. `internal fun migrateProvidersJsonToSchemaV2(rawProvidersJson: String): String?`.
    - For each provider JSON object:
      - If `endpoint` is a full OpenAI operation URL (`.../v1/audio/transcriptions` or `.../v1/chat/completions`), rewrite it to the base URL `.../v1`.
      - If `endpoint` contains Gemini operation path (already includes `:generateContent`), rewrite to base URL that ends at `/v1beta`.
      - Add missing fields:
        - `authMode` default: `NO_AUTH` for `WHISPER_ASR`, otherwise `API_KEY`.
        - For each model: add missing `kind` and `streamingPartialsSupported`.
          - Default `kind` inference:
            - If legacy endpoint contained `/audio/transcriptions` or provider type is `WHISPER_ASR` → `STT`
            - If provider type is `GEMINI` → `MULTIMODAL`
            - Otherwise → `TEXT`
          - Default `streamingPartialsSupported=false`.
    - Writes sanitized JSON back to `PROVIDERS_JSON` and sets `PROVIDER_SCHEMA_V2_MIGRATION_DONE=true`.

- Add unit tests in `ProviderSchemaV2MigrationTest.kt` (JUnit4) covering at minimum:
  - OpenAI legacy endpoint `https://api.openai.com/v1/audio/transcriptions` → migrated base URL `https://api.openai.com/v1`
  - OpenAI legacy endpoint `https://api.openai.com/v1/chat/completions` → migrated base URL `https://api.openai.com/v1`
  - Gemini legacy endpoint that already includes `:generateContent` → migrated base URL ending in `/v1beta`
  - Missing `authMode` injected with correct defaults based on provider type
  - Models missing `kind` / `streamingPartialsSupported` get defaults injected
  - Provider `id` remains unchanged in migrated JSON

- Wire migration execution:
  - In `SettingsNavigation` (SettingsScreen.kt), run `migrateProviderSchemaV2IfNeeded(context)` before setting `migrationReady=true`.
  - In `WhisperInputService.onCreate`, run `migrateProviderSchemaV2IfNeeded(this)` before any providers are consumed.
  </action>
  <verify>
JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew -p android testDebugUnitTest :app:assembleDebug
  </verify>
  <done>
- Existing installs with legacy per-operation endpoints load successfully.
- Saved provider endpoints are normalized to base URL semantics and new fields are present with safe defaults.
  </done>
</task>

<task type="auto">
  <name>Task 3: Update privacy disclosures to derive operation paths from base URLs + add unit tests</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatter.kt
android/app/src/test/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatterTest.kt
  </files>
  <action>
- Update `PrivacyDisclosureFormatter.resolveEndpoint(...)` so the disclosed `path` reflects the actual operation path derived from base URL:
  - Dictation (audio): basePath + `/audio/transcriptions` (for all non-`WHISPER_ASR` providers); `WHISPER_ASR` keeps its base path as-is.
  - Enhancement/Command (text):
    - `GEMINI`: keep existing `generateContent` path derivation.
    - otherwise: basePath + `/chat/completions`.

- Add `PrivacyDisclosureFormatterTest.kt` unit tests:
  - OpenAI base URL `https://api.openai.com/v1`:
    - Dictation path is `/v1/audio/transcriptions`
    - Enhancement path is `/v1/chat/completions`
  - Gemini base URL `https://generativelanguage.googleapis.com/v1beta` + model `gemini-1.5-pro`:
    - Enhancement path is `/v1beta/models/gemini-1.5-pro:generateContent`
  - Verify baseUrl formatting keeps scheme+host (+port when non-default).
  </action>
  <verify>
JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew -p android testDebugUnitTest :app:assembleDebug
  </verify>
  <done>
- Privacy & Safety disclosures remain accurate after moving providers to base URL semantics.
- Unit tests cover the derived paths for OpenAI and Gemini.
  </done>
</task>

</tasks>

<verification>
- Build: `JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew -p android :app:assembleDebug`
- Unit tests: `JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew -p android testDebugUnitTest`
</verification>

<success_criteria>
- PROV-01 foundation: providers persist in a schema aligned with Phase 2 decisions (type + base URL + auth mode).
- PROV-02 foundation: models carry kind + streaming partials support metadata.
- Legacy provider data migrates forward without breaking settings/IME startup.
</success_criteria>

<output>
After completion, create `.planning/phases/02-providers-models/02-01-SUMMARY.md`
</output>
