---
phase: 02-providers-models
plan: 04
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/WhisperTranscriber.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/SmartFixer.kt
autonomous: true
requirements:
  - PROV-03
  - PROV-04
  - PROV-05
  - PROV-06

must_haves:
  truths:
    - "Dictation uses the selected STT provider+model (not provider.models.first())"
    - "Enhancement uses the selected text provider+model; missing text selection does not block dictation"
    - "OpenAI-compatible base URLs work for STT (/audio/transcriptions) and text transforms (/chat/completions)"
    - "Gemini-compatible base URLs work for text transforms (/models/{model}:generateContent)"
  artifacts:
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt"
      provides: "Selection-aware provider/model resolution for dictation + enhancement"
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/WhisperTranscriber.kt"
      provides: "STT requests derived from provider base URL"
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/SmartFixer.kt"
      provides: "Text transform requests derived from provider base URL (OpenAI/Gemini)"
  key_links:
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt"
      to: "ACTIVE_STT_PROVIDER_ID/ACTIVE_STT_MODEL_ID"
      via: "DataStore preferences"
      pattern: "ACTIVE_STT_(PROVIDER|MODEL)_ID"
---

<objective>
Wire runtime behavior to Phase 2 selections and base-URL routing: dictation uses selected STT provider/model; enhancement uses selected text provider/model; OpenAI-compatible and Gemini-compatible base URLs are used to derive correct operation endpoints.

Purpose: Complete PROV-03/04/05/06 by making selections actually drive network requests.
Output: Updated IME selection resolution + OpenAI STT/text routing + Gemini text routing.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-providers-models/02-CONTEXT.md
@.planning/phases/02-providers-models/02-RESEARCH.md

@android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/WhisperTranscriber.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/SmartFixer.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resolve selected STT/text providers+models in WhisperInputService</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
  </files>
  <action>
- Dictation (STT):
  - Replace the current STT selection logic (`SPEECH_TO_TEXT_BACKEND` + provider.models.first()) with the Phase 2 keys:
    - providerId: `ACTIVE_STT_PROVIDER_ID`
    - modelId: `ACTIVE_STT_MODEL_ID`
  - If provider/model selection is missing or invalid:
    - Do not attempt network send.
    - Show a concise toast prompting user to complete setup in settings.

- Enhancement (Smart Fix):
  - Replace selection logic (`SMART_FIX_BACKEND`/`SMART_FIX_MODEL`) with:
    - providerId: `ACTIVE_TEXT_PROVIDER_ID`
    - modelId: `ACTIVE_TEXT_MODEL_ID`
  - If Smart Fix is enabled but text selection is missing/invalid:
    - Skip Smart Fix and commit raw transcription (do not block dictation).

- Command override is not executed yet (future phase), but selection keys should not break existing first-use disclosure flows.
  </action>
  <verify>
JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew -p android :app:assembleDebug
  </verify>
  <done>
- Dictation uses the selected STT provider+model.
- Smart Fix uses the selected text provider+model and cleanly falls back to raw transcript when text selection is missing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Derive OpenAI-compatible STT endpoint from base URL in WhisperTranscriber</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/WhisperTranscriber.kt
  </files>
  <action>
- Treat `provider.endpoint` as base URL.
- For non-`WHISPER_ASR` transcription:
  - Build request URL as `{baseUrl}/audio/transcriptions` (path-join safely; do not double-append if already migrated).
  - Apply auth based on provider authMode:
    - API_KEY: require non-empty `provider.apiKey` and send `Authorization: Bearer ...`.
    - NO_AUTH: do not send auth header.
- Keep safe error formatting and cancellation behavior from Phase 1 intact.
  </action>
  <verify>
JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew -p android :app:assembleDebug
  </verify>
  <done>
- OpenAI-compatible base URL works for STT via derived `/audio/transcriptions`.
- Auth mode is respected (API key required unless no-auth).
  </done>
</task>

<task type="auto">
  <name>Task 3: Derive OpenAI/Gemini text endpoints from base URL in SmartFixer (no-auth supported)</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/SmartFixer.kt
  </files>
  <action>
- Treat `provider.endpoint` as base URL.

- OpenAI-compatible (OPENAI/CUSTOM):
  - Build URL as `{baseUrl}/chat/completions`.
  - Auth mode:
    - API_KEY: send `Authorization: Bearer ...` (required).
    - NO_AUTH: omit auth header.

- Gemini-compatible (GEMINI):
  - Build URL as `{baseUrl}/models/{model}:generateContent`.
  - Auth mode:
    - API_KEY: include key without leaking it via logs (Phase 1 requires redacted verbose logs). If query-param auth is required, ensure the logging interceptor sanitizes the request line to redact `key=...`.
    - NO_AUTH: omit key.

- Keep Phase 1 safe error formatting and do not log response bodies.
  </action>
  <verify>
JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew -p android :app:assembleDebug
  </verify>
  <done>
- OpenAI-compatible base URL works for text transforms via derived `/chat/completions`.
- Gemini-compatible base URL works for text transforms via derived `:generateContent`.
- No-auth mode works for local/self-hosted endpoints.
  </done>
</task>

</tasks>

<verification>
- Build: `JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew -p android :app:assembleDebug`
</verification>

<success_criteria>
- PROV-03: dictation uses chosen STT provider+model.
- PROV-04: Smart Fix uses chosen text provider+model; command override keys exist and are safe.
- PROV-05: OpenAI-compatible STT + text work from base URL.
- PROV-06: Gemini-compatible text works from base URL.
</success_criteria>

<output>
After completion, create `.planning/phases/02-providers-models/02-04-SUMMARY.md`
</output>
