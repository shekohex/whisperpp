---
phase: 02-providers-models
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
autonomous: true
requirements:
  - PROV-01
  - PROV-02

must_haves:
  truths:
    - "User can add/edit providers with base URL + auth mode + API key handling rules"
    - "Provider type is immutable after creation; duplicate flow supports clone-and-edit"
    - "User can manage provider models including kind and streaming-partials flag"
  artifacts:
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt"
      provides: "Provider editor UI + model editor UI aligned with Phase 2 decisions"
  key_links:
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt"
      to: "android/app/src/main/java/com/github/shekohex/whisperpp/privacy/SecretsStore.kt"
      via: "set/clear stored API key by providerId"
      pattern: "SecretsStore\(" 
---

<objective>
Update provider + model management UX to match Phase 2 locked decisions: base URL prefill, strict validation before save, auth mode (API key vs no-auth), immutable type after creation, duplicate flow, duplicate warnings, and model metadata editing (kind + streaming partials).

Purpose: Deliver PROV-01/02 user-facing provider/model configuration behavior.
Output: Updated ProviderEdit UI and model editor controls.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-providers-models/02-CONTEXT.md
@.planning/phases/02-providers-models/02-RESEARCH.md

@android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement ProviderEdit UX rules (base URL, auth mode, immutable type, duplicate flow, strict validation)</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
  </files>
  <action>
- In `ProviderEditScreen`:
  - Rename field label from “Endpoint URL” to “Base URL”.
  - Base URL prefill by provider type:
    - OPENAI: `https://api.openai.com/v1`
    - GEMINI: `https://generativelanguage.googleapis.com/v1beta`
    - WHISPER_ASR: `https://YOUR_SERVER_IP:9000/asr`
    - CUSTOM: blank
  - Provider name default:
    - When creating a new provider, default name to `"{type} {host}"` where `host` is parsed from base URL (`toHttpUrlOrNull()?.host`).
    - Keep editable.
  - Provider type immutability:
    - When editing an existing provider, render type as read-only (no dropdown changes).
    - When creating a new provider, allow choosing type.
  - Auth mode:
    - Add a toggle/select for `authMode` (API key required by default, explicit no-auth mode).
    - If authMode=NO_AUTH, hide/disable API key input and allow save without a key.
    - If authMode=API_KEY, require a key to save (either already stored in SecretsStore or entered as a replacement).
  - Strict validation:
    - Disable Save until required fields are valid: non-blank name, valid base URL, auth mode constraints satisfied.
    - Base URL scheme policy:
      - Accept `https://` only (block save for `http://`).
  - Duplicate warnings:
    - If another provider exists with same `(type + normalized base URL)` (excluding current id), show a warning inline but still allow save.
  - Duplicate action:
    - Add a "Duplicate" action that opens a clone-and-edit flow:
      - Creates a new provider draft prefilled from the current provider (new id).
      - User can edit fields before saving.
      - Type should be editable in the duplicated draft (new provider), then locked after saving.
  </action>
  <verify>
JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew -p android :app:assembleDebug
  </verify>
  <done>
- Provider editor matches Phase 2 locked decisions: base URL prefill/override, strict save validation, auth mode, immutable type after creation, duplicate flow, duplicates warning.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update model editor to capture kind + streaming-partials support</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
  </files>
  <action>
- Update `ModelEditCard` UI to edit new model metadata:
  - Add a kind picker for `ModelKind` with values: STT / TEXT / MULTIMODAL.
  - Add a toggle for `streamingPartialsSupported`:
    - Default off for new STT-capable models.
    - Only show the toggle when kind is STT or MULTIMODAL.
  - Keep existing `Thinking Model` checkbox (do not remove existing capability).

- Update "Add Model" behavior:
  - New models should default to kind=TEXT and streamingPartialsSupported=false.
  </action>
  <verify>
JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew -p android :app:assembleDebug
  </verify>
  <done>
- Models in settings can be edited with kind + streaming-partials capability metadata.
  </done>
</task>

</tasks>

<verification>
- Build: `JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew -p android :app:assembleDebug`
</verification>

<success_criteria>
- PROV-01: Providers can be created/edited with base URL + auth mode + API key rules and type immutability.
- PROV-02: Models can be managed with kind + streaming partials supported flag.
</success_criteria>

<output>
After completion, create `.planning/phases/02-providers-models/02-02-SUMMARY.md`
</output>
