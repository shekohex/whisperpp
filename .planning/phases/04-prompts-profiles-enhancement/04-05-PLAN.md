---
phase: 04-prompts-profiles-enhancement
plan: 05
type: execute
wave: 3
depends_on:
  - "04-03"
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
autonomous: true
requirements:
  - PROF-01
  - PROF-02

must_haves:
  truths:
    - "User can edit a global base prompt used for enhancement prompting"
    - "User can create, edit, and delete prompt profiles (name + prompt append)"
  artifacts:
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt"
      provides: "Settings routes and UI for base prompt + prompt profile CRUD"
  key_links:
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt"
      to: "android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt"
      via: "persist base prompt and prompt profiles"
      pattern: "SettingsRepository\(dataStore\)"
---

<objective>
Add Settings UI for configuring the Phase 4 global base prompt and managing a library of prompt profiles.

Purpose: Make prompting configurable without touching per-app mappings yet.
Output: New settings routes + screens for base prompt + prompt profile CRUD.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-prompts-profiles-enhancement/04-CONTEXT.md

@android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add settings routes and screen for editing the global base prompt</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
  </files>
  <action>
- In `SettingsScreen` sealed routes, add a Phase 4 destination for prompts/profiles (route naming discretion).
- Add a navigation entry from the main settings screen (e.g., in a "Prompts" or "Enhancement" group).

- Implement a screen that:
  - Shows the current global base prompt value.
  - Allows editing via a multi-line text field.
  - Persists to `SettingsRepository` immediately or via an explicit Save action (discretion).
  - Trims but does not otherwise rewrite user content.

- Avoid introducing new dependencies; follow existing Compose + DataStore patterns in this file.
  </action>
  <verify>
    <automated>JAVA_HOME=/home/coder/.local/jdks/jdk-17.0.18+8 ./android/gradlew -p android :app:assembleDebug</automated>
  </verify>
  <done>
- Base prompt is editable and persisted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement prompt profile list + create/edit/delete screens (name + prompt append)</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
  </files>
  <action>
- Add prompt profile management UI:
  - List existing profiles (name + short preview of append).
  - Create profile (generate id, name required, append optional).
  - Edit profile (rename, edit append text).
  - Delete profile.

- Deletion behavior must be compatible with locked Phase 4 mapping semantics:
  - Do NOT automatically delete app mappings that reference the deleted profile; mappings must be retained and flagged later.

- Persist all changes via `SettingsRepository` promptProfiles flow/save helpers.
  </action>
  <verify>
    <automated>JAVA_HOME=/home/coder/.local/jdks/jdk-17.0.18+8 ./android/gradlew -p android :app:assembleDebug</automated>
  </verify>
  <done>
- Profiles can be created/edited/deleted.
- Deleting a profile does not delete mappings.
  </done>
</task>

</tasks>

<verification>
- `:app:assembleDebug` passes.
</verification>

<success_criteria>
- Users can configure a base prompt and manage prompt profiles from settings.
</success_criteria>

<output>
After completion, create `.planning/phases/04-prompts-profiles-enhancement/04-05-SUMMARY.md`
</output>
