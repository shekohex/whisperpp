---
phase: 04-prompts-profiles-enhancement
plan: 04
type: execute
wave: 3
depends_on:
  - "04-01"
  - "04-02"
  - "04-03"
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/SmartFixer.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/dictation/DictationController.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/dictation/EnhancementUndoEntry.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
autonomous: true
requirements:
  - ENH-01
  - ENH-02
  - ENH-03
  - ENH-04
  - PROF-03
  - PROF-04

must_haves:
  truths:
    - "Dictation STT provider/model selection is resolved via app/language/global precedence (with invalid fallback + non-blocking warning) and is used for dictation start + transcription"
    - "After dictation send completes, raw transcript is inserted immediately"
    - "Enhancement auto-runs after every successful dictation stop (unless empty/punctuation-only)"
    - "On enhancement success, the originally inserted raw segment is replaced in place (segment-scoped), caret ends at segment end"
    - "If enhancement cannot run or fails, raw stays inserted and a detailed notice is shown each time (policy block uses info-style)"
    - "If focus/app changes before enhancement returns, replacement is not applied"
    - "User can undo the latest enhancement replacement to restore the exact raw dictated segment (even after manual edits)"
  artifacts:
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/dictation/EnhancementUndoEntry.kt"
      provides: "Single latest enhancement undo contract (independent from dictation undo)"
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/dictation/DictationController.kt"
      provides: "Raw insertion capture + segment replacement + enhancement undo apply"
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt"
      provides: "Raw-first enhancement orchestration using runtime resolver and EnhancementRunner"
  key_links:
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt"
      to: "android/app/src/main/java/com/github/shekohex/whisperpp/dictation/DictationController.kt"
      via: "raw insert then replace"
      pattern: "replace.*Segment|insert.*Raw"
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt"
      to: "android/app/src/main/java/com/github/shekohex/whisperpp/data/RuntimeSelectionResolver.kt"
      via: "resolve effective prompt + selections"
      pattern: "RuntimeSelectionResolver"
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt"
      to: "whisperTranscriber.startAsync"
      via: "use effective STT provider/model"
      pattern: "startAsync\("
---

<objective>
Convert enhancement to a raw-first, replace-later pipeline: insert raw immediately, run enhancement with the effective prompt and effective TEXT model/provider, then replace the original dictated segment in place with undo back to raw.

Purpose: Deliver Phase 4 core enhancement UX safely (segment-scoped replacement, focus-safety, explicit notices, undo).
Output: Updated dictation controller + IME wiring + keyboard UI for enhancement notices and enhancement-undo.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-prompts-profiles-enhancement/04-CONTEXT.md
@.planning/phases/04-prompts-profiles-enhancement/04-RESEARCH.md

@android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/dictation/DictationController.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add raw insertion capture + segment replacement + enhancement undo (single latest)</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/dictation/DictationController.kt
android/app/src/main/java/com/github/shekohex/whisperpp/dictation/EnhancementUndoEntry.kt
  </files>
  <action>
- Create `EnhancementUndoEntry.kt` in `com.github.shekohex.whisperpp.dictation` representing a single latest enhancement replacement:
  - MUST store `focusKey` and the segment bounds used for replacement (start + end-at-replace) and the `rawText` to restore.
  - MUST NOT rely on "selected text equals expected" validation (locked: undo remains valid even after manual edits).

- Update `DictationController` to support Phase 4 segment operations:
   - When final transcript arrives, controller MUST be able to insert raw immediately and capture the insertion segment bounds. Implement an explicit capture contract that can be used later for replacement (exact names discretion, but keep it explicit and testable):
     - Example: `data class CapturedDictationSegment(focusKey: FocusKey, start: Int, endAtInsert: Int, rawText: String)`.
     - Capture `start` from the selection snapshot before insert.
     - Capture `endAtInsert = start + rawText.length` based on the raw text length.
     - These captured bounds MUST be used for replacement even if the user edits the raw segment before enhancement returns (locked: still force replacement).
   - Add an API for the service to request replacement of that captured segment with enhanced text.
  - Replacement MUST:
    - Target the captured segment bounds (`start..endAtInsert`) (not current caret).
    - Be segment-scoped only (select exactly start..end and replace only that).
    - Move caret to end of replaced segment (locked).
    - Create/update the single latest `EnhancementUndoEntry`.
    - NOT validate current contents of the segment (no text-equality guard); replacement must proceed even if the user edited raw (locked).
  - If current focus no longer matches captured focus (FocusKey mismatch), replacement MUST be dropped (locked).

- Add enhancement undo apply API:
  - Restores raw text by overwriting the stored replacement range; do not validate current contents.
  - Clear the enhancement undo entry after applying.
  - Clear enhancement undo entry on the next dictation session/replacement.

- Do NOT add any redo action.
  </action>
  <verify>
    <automated>JAVA_HOME=/home/coder/.local/jdks/jdk-17.0.18+8 ./android/gradlew -p android :app:assembleDebug</automated>
  </verify>
  <done>
- Controller supports raw insert capture, segment replacement, and single-entry enhancement undo, with focus-safety drop behavior.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewire WhisperInputService to apply resolver precedence for BOTH dictation STT and enhancement TEXT (raw-first) + notices</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
android/app/src/main/java/com/github/shekohex/whisperpp/SmartFixer.kt
  </files>
  <action>

- Apply resolver precedence to dictation STT selection (PROF-03/PROF-04) and use it for dictation start/transcription:
  - In the dictation start path (`onMicAction` when transitioning Ready -> start), call `RuntimeSelectionResolver.resolve(...)` using:
    - current app packageName from the focused editor (`currentInputEditorInfo?.packageName`) and the current language code preference.
    - providers list, language defaults (`SettingsRepository.profiles`), prompt profiles, app mappings, global base prompt, and global default selections.
  - Use the resolved **effective STT providerId/modelId** (after invalid fallback) for:
    - Dictation disclosure derivation (provider + model).
    - Streaming capability check (selected model’s streamingPartialsSupported).
    - Realtime client selection (only if effective STT provider supports realtime).
  - If resolver emits warnings (invalid override/model-kind mismatch/missing profile): surface a non-blocking warning notice (info-style) and proceed using the effective STT selection.
  - If no valid STT provider/model can be resolved at all: show setup-required notice/toast and refuse to start dictation.
  - Ensure `startTranscription(...)` uses the same effective STT provider/model for this session (store it at dictation start and reuse; do not read raw prefs again for STT selection).

- Update `transcriptionCallback(...)` to follow locked Phase 4 raw-first enhancement pipeline (TEXT channel):
  1) Determine `rawText` (existing best-effort finalize behavior must remain).
  2) If blocked by secure-field/app-policy at send time: do NOT run enhancement; insert raw (or copy to clipboard if unsafe) and show reason as info-style.
  3) Insert raw immediately via `DictationController` and capture the segment bounds.
  4) If transcript is empty/punctuation-only: skip enhancement and finish.
  5) Resolve effective runtime config using `RuntimeSelectionResolver` (same inputs as above) and use resolved **TEXT provider/model** + **effective prompt**:
     - If resolved provider/model is invalid: fallback down precedence and surface a non-blocking warning notice (do not block insertion).
  6) Run enhancement using `EnhancementRunner` around the SmartFix call:
     - Single-flight: while enhancement is running, keep keyboard state non-Ready so new dictation cannot start (locked).
     - Timeout + one quick retry for transient failures, then fallback.
  7) On success: replace the captured raw segment using controller replacement API.
  8) On failure/unavailable: keep raw and show detailed notice each time including provider + model (locked);
     - If reason is intentional policy block, notice MUST be info-style (not error-style).

- Update SmartFix invocation to use the effective prompt (base + profile append + app append) instead of `provider.prompt` / `SMART_FIX_PROMPT` precedence.
  - Keep provider temperature behavior unchanged.
  - Keep safe HTTP error surfacing behavior (provider + status + host) when possible; do not log secrets.

- Ensure focus-safety for replacement:
  - If focus/app changes before enhancement result returns, do not apply replacement (locked).

- Keep trying enhancement on every dictation (locked): no auto-disable after failures.
  </action>
  <verify>
    <automated>JAVA_HOME=/home/coder/.local/jdks/jdk-17.0.18+8 ./android/gradlew -p android testDebugUnitTest</automated>
  </verify>
  <done>
- Raw-first enhancement pipeline is implemented and builds.
- Enhancement uses effective prompt and effective TEXT selection.
- No crashes on invalid overrides; fallback + notice path exists.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add IME UI support for enhancement notices and enhancement-undo quick action</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
  </files>
  <action>
- Extend IME Compose state in `WhisperInputService` and props to `KeyboardScreen` to support:
  - A transient enhancement notice (message + style: info vs error) that auto-dismisses after a short duration (discretion).
  - An enhancement undo quick action/chip that is shown only when an enhancement replacement was applied (locked).

- Keyboard UI requirements:
  - Enhancement undo quick action MUST remain available until the next dictation/replacement (locked).
  - When tapped, call into the controller enhancement undo API and restore raw in-place.
  - Do NOT implement any redo affordance.

- Notices:
  - On each fallback, show a detailed notice including provider + model (locked). Exact wording is discretion.
  - Policy-block notice must be info-style; other failures can be error-style (locked).
  </action>
  <verify>
    <automated>JAVA_HOME=/home/coder/.local/jdks/jdk-17.0.18+8 ./android/gradlew -p android :app:assembleDebug</automated>
  </verify>
  <done>
- Keyboard shows enhancement undo action after successful replacement and shows notices on fallback.
- Redo is not present.
  </done>
</task>

</tasks>

<verification>
- `:app:assembleDebug` passes.
- `testDebugUnitTest` passes.
</verification>

<success_criteria>
- Enhancement runs automatically after dictation send, replaces the inserted raw segment on success, and supports single-entry enhancement undo to restore raw.
</success_criteria>

<output>
After completion, create `.planning/phases/04-prompts-profiles-enhancement/04-04-SUMMARY.md`
</output>
