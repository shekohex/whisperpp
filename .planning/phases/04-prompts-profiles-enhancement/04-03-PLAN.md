---
phase: 04-prompts-profiles-enhancement
plan: 03
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt
  - android/app/src/test/java/com/github/shekohex/whisperpp/data/PromptProfilesPersistenceTest.kt
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03

must_haves:
  truths:
    - "Global base prompt is persisted and retrievable"
    - "Prompt profiles and per-app mappings persist via DataStore JSON and survive app restarts"
    - "Stored prompt/profile/mapping payloads are sanitized on load and never crash parsing"
  artifacts:
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt"
      provides: "Persistence for base prompt + prompt profiles + app mappings"
    - path: "android/app/src/test/java/com/github/shekohex/whisperpp/data/PromptProfilesPersistenceTest.kt"
      provides: "Unit coverage for JSON parsing + sanitization helpers"
  key_links:
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt"
      to: "android/app/src/main/java/com/github/shekohex/whisperpp/data/PromptProfiles.kt"
      via: "Gson encode/decode"
      pattern: "TypeToken<List<PromptProfile>>"
---

<objective>
Persist Phase 4 prompt settings: a global base prompt, a prompt profile library, and per-app mappings (including append mode and optional STT/TEXT overrides).

Purpose: Make profiles/mappings configurable and durable before wiring them into the IME.
Output: New DataStore preference keys + repository flows/save helpers + unit tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-prompts-profiles-enhancement/04-CONTEXT.md
@.planning/phases/04-prompts-profiles-enhancement/04-RESEARCH.md

@android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/data/ProviderModels.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DataStore keys + flows + save helpers for base prompt, prompt profiles, and per-app mappings</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt
  </files>
  <action>
- In `SettingsRepository.kt`, add new preferences keys (stringPreferencesKey / booleanPreferencesKey) for Phase 4:
  - Global base prompt string.
  - Prompt profiles JSON string (list of `PromptProfile`).
  - App mappings JSON string (list of `AppPromptMapping`).
  - Migration flag for this schema version (same pattern as other migrations).

- Add repository Flows:
  - `globalBasePrompt: Flow<String>`
  - `promptProfiles: Flow<List<PromptProfile>>`
  - `appPromptMappings: Flow<List<AppPromptMapping>>`

- Add save/upsert helpers:
  - Save global base prompt.
  - Save full profile list; optionally helpers to upsert/delete by id.
  - Save full mapping list; optionally helpers to upsert/delete by package.

- Sanitization rules (apply on load, returning safe defaults):
  - Strip blank ids/names/package names.
  - Trim whitespace.
  - Keep mappings referencing missing profiles (do NOT delete them); these will be flagged at runtime/UI.
  - If an override providerId/modelId is blank, keep it blank (resolver will cascade).
  - On JSON parse failure, return empty lists/empty prompt.

- Do NOT modify export/import format in this phase.
  </action>
  <verify>
    <automated>JAVA_HOME=/home/coder/.local/jdks/jdk-17.0.18+8 ./android/gradlew -p android :app:assembleDebug</automated>
  </verify>
  <done>
- New keys and flows exist; JSON parsing is defensive and sanitized.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for sanitization + parsing fallbacks for prompt profiles and mappings</name>
  <files>
android/app/src/test/java/com/github/shekohex/whisperpp/data/PromptProfilesPersistenceTest.kt
  </files>
  <action>
- Add JUnit4 tests that validate sanitization helpers (prefer pure functions / internal helpers) for:
  - Blank/whitespace-only package names are dropped.
  - Blank/whitespace-only profile ids are dropped.
  - Orphan mappings (profileId references missing profile) are retained.
  - JSON parse error returns empty list instead of throwing.

- Keep tests deterministic and independent of Android runtime; mirror patterns in `ProviderSelectionsMigrationTest` (using `Gson` + pure helper functions).
  </action>
  <verify>
    <automated>JAVA_HOME=/home/coder/.local/jdks/jdk-17.0.18+8 ./android/gradlew -p android testDebugUnitTest</automated>
  </verify>
  <done>
- Unit tests pass and protect against corrupted JSON payloads.
  </done>
</task>

</tasks>

<verification>
- `testDebugUnitTest` passes.
</verification>

<success_criteria>
- Base prompt + prompt profiles + app mappings are persisted via SettingsRepository and can be used by both settings UI and the IME.
</success_criteria>

<output>
After completion, create `.planning/phases/04-prompts-profiles-enhancement/04-03-SUMMARY.md`
</output>
