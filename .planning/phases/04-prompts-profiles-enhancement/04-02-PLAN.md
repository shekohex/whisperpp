---
phase: 04-prompts-profiles-enhancement
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/dictation/EnhancementRunner.kt
  - android/app/src/test/java/com/github/shekohex/whisperpp/dictation/EnhancementRunnerTest.kt
autonomous: true
requirements:
  - ENH-01

must_haves:
  truths:
    - "Empty or punctuation-only transcripts are never sent for enhancement"
    - "Enhancement attempts are bounded by timeout(s) and do at most one retry for transient failures"
    - "Enhancement produces a structured outcome describing success/skip/failure and attempts used"
  artifacts:
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/dictation/EnhancementRunner.kt"
      provides: "Timeout + retry envelope and skip policy for enhancement"
    - path: "android/app/src/test/java/com/github/shekohex/whisperpp/dictation/EnhancementRunnerTest.kt"
      provides: "Unit coverage for punctuation-skip + timeout + retry behavior"
  key_links:
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/dictation/EnhancementRunner.kt"
      to: "kotlinx.coroutines.withTimeout"
      via: "bounded attempt"
      pattern: "withTimeout"
---

<objective>
Implement an enhancement runner that enforces Phase 4 enhancement policy constraints (punctuation-only skip, balanced timeout, one quick retry on transient failures) and returns structured results for later UI notices.

Purpose: Keep enhancement behavior deterministic and testable before wiring into the IME.
Output: EnhancementRunner + unit tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-prompts-profiles-enhancement/04-CONTEXT.md
@.planning/phases/04-prompts-profiles-enhancement/04-RESEARCH.md

@android/app/src/main/java/com/github/shekohex/whisperpp/SmartFixer.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add EnhancementRunner with punctuation-only skip + timeout + single quick retry</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/dictation/EnhancementRunner.kt
  </files>
  <action>
- Create `EnhancementRunner` in `com.github.shekohex.whisperpp.dictation`.

- Locked behaviors to implement:
  - Auto-run policy support: runner MUST be usable for “run on every successful dictation stop”.
  - Skip policy: if transcript is empty OR only punctuation/whitespace, return `Skipped` and do not call the provided enhancer.
  - Timeout strategy: enforce a bounded wait for enhancement ("balanced wait").
  - Retry policy: for transient failures (including rate-limit), do one quick retry, then return `Failed`.
  - Cancellation must be treated as benign (return `Cancelled` or `Failed(Cancelled)` without throwing).

- Discretion (choose values, but make them configurable via a config object so IME can tune later):
  - Balanced timeout duration.
  - Quick-retry timeout duration.
  - Transient failure classifier (e.g., explicit exception type or message pattern containing HTTP 429).

- API shape (must be simple to call from `WhisperInputService`):
  - `suspend fun run(rawText: String, enhance: suspend (String) -> String): EnhancementOutcome`
  - `EnhancementOutcome` MUST be a sealed type with:
    - `Succeeded(enhancedText, attempts)`
    - `Skipped(reason)`
    - `Failed(reason, attempts)`
  - Provide a `reason` enum or sealed type that can later map to info-style vs error-style notices.
  </action>
  <verify>
    <automated>JAVA_HOME=/home/coder/.local/jdks/jdk-17.0.18+8 ./android/gradlew -p android :app:assembleDebug</automated>
  </verify>
  <done>
- EnhancementRunner exists, compiles, and exposes a structured outcome API.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add unit tests for skip + timeout + transient retry envelope</name>
  <files>
android/app/src/test/java/com/github/shekohex/whisperpp/dictation/EnhancementRunnerTest.kt
  </files>
  <action>
- Add JUnit4 tests covering:
  - Punctuation-only skip (e.g., "...", "!!!", "\n\t") does not call enhancer.
  - Timeout path returns `Failed` after (at most) 2 attempts (balanced + quick retry).
  - Transient error triggers exactly one retry.
  - Non-transient error does not retry.
  - Cancellation does not throw.

- Tests MUST use short timeouts to keep `testDebugUnitTest` fast.
  </action>
  <verify>
    <automated>JAVA_HOME=/home/coder/.local/jdks/jdk-17.0.18+8 ./android/gradlew -p android testDebugUnitTest</automated>
  </verify>
  <done>
- Unit tests pass and enforce Phase 4 enhancement envelope behavior.
  </done>
</task>

</tasks>

<verification>
- `testDebugUnitTest` passes.
</verification>

<success_criteria>
- Enhancement policy (skip/timeout/retry) is encapsulated in one tested runner, ready for IME wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/04-prompts-profiles-enhancement/04-02-SUMMARY.md`
</output>
