---
phase: 04-prompts-profiles-enhancement
plan: 07
type: execute
wave: 4
depends_on:
  - "04-04"
files_modified:
  - android/app/src/test/java/com/github/shekohex/whisperpp/dictation/FakeInputConnection.kt
  - android/app/src/test/java/com/github/shekohex/whisperpp/dictation/DictationEnhancementReplacementTest.kt
autonomous: true
requirements:
  - ENH-03
  - ENH-04

must_haves:
  truths:
    - "Segment replacement uses the originally captured insertion bounds (not current caret) and does not modify surrounding text"
    - "If focus/app changes before enhancement returns, replacement is dropped"
    - "Enhancement undo restores the exact raw transcript into the original replacement range even after user edits"
  artifacts:
    - path: "android/app/src/test/java/com/github/shekohex/whisperpp/dictation/FakeInputConnection.kt"
      provides: "Minimal fake InputConnection for controller tests"
    - path: "android/app/src/test/java/com/github/shekohex/whisperpp/dictation/DictationEnhancementReplacementTest.kt"
      provides: "Unit tests for captured-range replacement + focus-safety + enhancement undo"
  key_links:
    - from: "android/app/src/test/java/com/github/shekohex/whisperpp/dictation/DictationEnhancementReplacementTest.kt"
      to: "android/app/src/main/java/com/github/shekohex/whisperpp/dictation/DictationController.kt"
      via: "calls enhancement segment capture/replace/undo APIs"
      pattern: "DictationController"
---

<objective>
Add focused unit tests that lock in the high-risk Phase 4 behaviors: captured-range replacement, focus-safety drop, and single-entry enhancement undo restoring raw.

Purpose: Prevent regressions in segment-scoped replacement + undo semantics across arbitrary editors.
Output: FakeInputConnection test harness + controller-level unit tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-prompts-profiles-enhancement/04-CONTEXT.md

@android/app/src/main/java/com/github/shekohex/whisperpp/dictation/DictationController.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/dictation/FocusKey.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a minimal FakeInputConnection for deterministic range/selection tests</name>
  <files>
android/app/src/test/java/com/github/shekohex/whisperpp/dictation/FakeInputConnection.kt
  </files>
  <action>
- Create `FakeInputConnection` implementing `android.view.inputmethod.InputConnection` with ONLY the behavior needed by the Phase 4 controller replacement/undo paths:
  - Maintain an internal mutable text buffer and a (start,end) selection.
  - Implement: `getExtractedText(...)` returning selectionStart/selectionEnd, `setSelection(...)`, `getSelectedText(...)`, `commitText(...)`, `setComposingText(...)`, `finishComposingText()`, `beginBatchEdit()`, `endBatchEdit()`.
  - All other methods can be stubbed with safe defaults (return false/null) but MUST NOT throw.

- Add small helpers on the fake for tests (public functions):
  - `setText(String)` and `getText(): String`
  - `setSelection(start, end)` and `getSelection(): Pair<Int, Int>`
  - Optional: `replaceRange(start, end, replacement)` to simulate user edits.
  </action>
  <verify>
    <automated>JAVA_HOME=/home/coder/.local/jdks/jdk-17.0.18+8 ./android/gradlew -p android testDebugUnitTest</automated>
  </verify>
  <done>
- FakeInputConnection compiles in local unit tests and supports deterministic text+selection mutation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add controller-level unit tests for captured-range replacement, focus mismatch drop, and enhancement undo</name>
  <files>
android/app/src/test/java/com/github/shekohex/whisperpp/dictation/DictationEnhancementReplacementTest.kt
  </files>
  <action>
- Add JUnit4 tests (pure JVM unit tests) that exercise the enhancement segment APIs added in `04-04` (names can differ, but the behaviors MUST be asserted):

  1) Captured-range replacement uses original bounds:
     - Arrange a controller with deps using `FakeInputConnection` and a known initial text (prefix + caret + suffix).
     - Simulate focus with a real `FocusKey` and a `SendToken`.
     - Call the controller method that inserts raw transcript and returns a captured segment handle (the explicit capture contract from 04-04 Task 1).
     - Mutate the fake text/selection to simulate a user edit occurring before enhancement returns.
     - Apply enhancement replacement using the captured segment handle.
     - Assert:
       - Only the originally captured range is replaced (prefix/suffix unchanged).
       - Caret ends at the end of the replaced segment.

  2) Focus mismatch drop:
     - Insert raw and capture the segment.
     - Change controller focus via `onFocusChanged(...)` to a different FocusKey.
     - Attempt replacement.
     - Assert text is unchanged and no enhancement undo is available/created.

  3) Enhancement undo restores raw even after edits:
     - Insert raw and apply replacement.
     - Mutate the fake to simulate user edits on the enhanced text.
     - Trigger enhancement undo.
     - Assert the raw transcript is restored exactly within the stored replacement range and caret is positioned sensibly (end of restored segment is acceptable).

- Keep tests deterministic (no delays/coroutines) and do not require Android instrumentation.
  </action>
  <verify>
    <automated>JAVA_HOME=/home/coder/.local/jdks/jdk-17.0.18+8 ./android/gradlew -p android testDebugUnitTest</automated>
  </verify>
  <done>
- Unit tests pass and lock in captured-range replacement, focus-safety drop, and enhancement undo restore semantics.
  </done>
</task>

</tasks>

<verification>
- `testDebugUnitTest` passes.
</verification>

<success_criteria>
- Regressions in Phase 4 segment replacement and enhancement undo semantics are caught by fast unit tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-prompts-profiles-enhancement/04-07-SUMMARY.md`
</output>
