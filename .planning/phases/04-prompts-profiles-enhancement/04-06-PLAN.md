---
phase: 04-prompts-profiles-enhancement
plan: 06
type: execute
wave: 4
depends_on:
  - "04-05"
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
autonomous: true
requirements:
  - PROF-02
  - PROF-03
  - PROF-04

must_haves:
  truths:
    - "User can map apps (by package name) to a prompt profile via a searchable installed-app list with manual package fallback"
    - "User can bulk multi-select apps and assign one profile to multiple apps in one action"
    - "Per-app mapping can set append/no-append, edit an app-specific append text, and override STT and TEXT provider/model selections"
    - "User can configure per-language defaults for STT and TEXT selections independently"
    - "If a mapped profile is deleted, mapping rows remain, are flagged, and runtime falls back to global behavior"
  artifacts:
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt"
      provides: "Settings UI for app mappings + per-language defaults"
  key_links:
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt"
      to: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreen.kt"
      via: "reuse installed app picker patterns"
      pattern: "queryIntentActivities"
---

<objective>
Add Settings UI for per-app profile mappings (searchable app picker, manual package fallback, bulk multi-select assign) including per-app overrides, plus a per-language defaults editor for STT/TEXT selections.

Purpose: Complete user-configurable profiles/mappings/defaults required for Phase 4 runtime behavior.
Output: Settings screens for app mappings + mapping detail + language defaults.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-prompts-profiles-enhancement/04-CONTEXT.md
@.planning/phases/04-prompts-profiles-enhancement/04-RESEARCH.md

@android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreen.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement app mapping picker with search + manual package add + bulk multi-select assign</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
  </files>
  <action>
- Add settings destinations for:
  - App mappings list/picker screen.
  - Mapping detail screen (can be separate route with encoded packageName).

- App picker requirements (locked):
  - Must list installed apps (include user + system apps) using the existing launcher-intent query pattern (see `PrivacySafetyScreen.kt`).
  - Must be searchable by label and package name.
  - Must support manual package-name add for apps not listed.
  - Must keep mappings by package name even if the app is uninstalled (merged list includes unmapped entries from stored mappings).
  - Must support bulk multi-select: select many apps, then assign a single prompt profile to all in one action.

- Persist mappings via `SettingsRepository` appPromptMappings helpers.
  </action>
  <verify>
    <automated>JAVA_HOME=/home/coder/.local/jdks/jdk-17.0.18+8 ./android/gradlew -p android :app:assembleDebug</automated>
  </verify>
  <done>
- App mapping picker exists with search, manual add, and bulk assign.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement mapping detail overrides and per-language defaults editor (STT/TEXT independently)</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
  </files>
  <action>
- Mapping detail requirements (locked):
  - Select one profile per app package.
  - Allow per-app append control:
    - Default append behavior uses profile append + app append.
    - Allow explicit `NO_APPEND` (disables both profile + app append, keeps base prompt).
  - App-specific append editor (locked: composition includes app-specific append):
    - Add an editable multi-line text field for the mapping's `appPromptAppend` (name discretion, but persist to mapping model).
    - When `appendMode == NO_APPEND`, hide or disable this field (and any profile-append preview) while still keeping base prompt active.
    - Allow clearing to blank to remove app-specific append.
  - Allow per-app STT override (provider/model) and per-app TEXT override (provider/model).
  - Overrides must support partial fields (provider or model unset) and inherit down precedence.

- Deleted/missing profile handling (locked):
  - If a mapping references a missing profile, show a flagged state in the mapping list/detail.
  - Do NOT block editing other fields; do NOT delete the mapping automatically.

- Provider/model picker behavior:
  - Reuse the existing two-step provider→model selection UI patterns in Settings.
  - Enforce kind compatibility in UI where practical (STT vs TEXT).

- Per-language defaults editor (locked PROF-04):
  - UI to add/edit/remove `LanguageProfile` entries keyed by languageCode.
  - For each language, allow picking STT provider+model and TEXT provider+model independently.
  - Persist via existing `SettingsRepository.profiles` / `saveProfiles`.

- Do NOT add any redo feature.
  </action>
  <verify>
    <automated>JAVA_HOME=/home/coder/.local/jdks/jdk-17.0.18+8 ./android/gradlew -p android :app:assembleDebug</automated>
  </verify>
  <done>
- Mapping detail supports append/no-append and STT/TEXT overrides.
- Mapping detail supports editing an app-specific append text that persists per app.
- Language defaults editor exists and persists.
- Deleted-profile mappings are flagged and retained.
  </done>
</task>

</tasks>

<verification>
- `:app:assembleDebug` passes.
</verification>

<success_criteria>
- Users can create app→profile mappings (bulk + manual) with per-app overrides and per-language defaults, completing Phase 4 configurability.
</success_criteria>

<output>
After completion, create `.planning/phases/04-prompts-profiles-enhancement/04-06-SUMMARY.md`
</output>
