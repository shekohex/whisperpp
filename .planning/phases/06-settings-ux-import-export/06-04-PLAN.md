---
phase: 06-settings-ux-import-export
plan: 04
type: execute
wave: 3
depends_on:
  - "06-02"
  - "06-03"
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/BackupRestoreScreen.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsHomeScreen.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt
  - android/app/src/main/res/values/strings.xml
autonomous: true
requirements:
  - UI-01
  - SET-01
  - SET-02

must_haves:
  truths:
    - "Export requires password entry plus confirmation and clearly lists included categories, including sensitive contents"
    - "Import flow goes file pick -> overwrite/merge bottom sheet -> password -> preview -> explicit confirmation"
    - "Merge mode lets users include or exclude whole categories before apply"
    - "After restore, the user stays in settings and sees a completion summary, skipped-item report, and repair checklist"
  artifacts:
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/BackupRestoreScreen.kt"
      provides: "Material 3 export/import UI flow with password sheets, preview, report, and completion summary"
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsHomeScreen.kt"
      provides: "Backup & restore entry point and post-restore banner/status integration"
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt"
      provides: "Navigation route for the new backup screen"
  key_links:
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/BackupRestoreScreen.kt"
      to: "android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsBackupRepository.kt"
      via: "export/analyze/apply calls"
      pattern: "SettingsBackupRepository"
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/BackupRestoreScreen.kt"
      to: "androidx.activity.result.contract.ActivityResultContracts"
      via: "SAF create/open document launchers"
      pattern: "CreateDocument|OpenDocument"
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsHomeScreen.kt"
      to: "android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsBackupRepository.kt"
      via: "post-restore summary or repair-state banner"
      pattern: "repair|restore"
---

<objective>
Ship the user-facing Material 3 backup/restore flow on top of the new backup domain: category-transparent export, safe import preview, merge/overwrite choice, and completion reporting that keeps users inside settings.

Purpose: Complete Phase 6 requirements by making encrypted backup/restore usable and understandable in settings.
Output: New backup screen, home entry point, and full import/export UI flow wired to the backup repository.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-settings-ux-import-export/06-CONTEXT.md
@.planning/phases/06-settings-ux-import-export/06-RESEARCH.md

@android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsHomeScreen.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsHelp.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsBackupRepository.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the dedicated Backup & restore screen and password-protected export flow</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/BackupRestoreScreen.kt
android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsHomeScreen.kt
android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt
android/app/src/main/res/values/strings.xml
  </files>
  <action>
- Add a dedicated nested `Backup & restore` destination instead of keeping import/export as two loose home rows.

- Add a home-card entry for backup/restore with one concise status line (for example: last export time, no backup yet, or repair attention needed). Keep it below setup-critical configuration cards, not above them.

- Implement export UX on the backup screen using Material 3 components only:
  - show the exact included categories from Plan 06-01, including an explicit sensitive-content warning for `provider_credentials`
  - require password entry and password confirmation before any file is written; there is no plain export path
  - show backup identity fields that will be written (app version + export timestamp)
  - write the encrypted file through SAF `CreateDocument`; use a stable extension such as `.whisperpp-backup`
  - after success/failure, keep the user on the backup screen, update an inline status summary, and show toast feedback

- Reuse the shared contextual-help affordance for the backup route so the top-bar help explains export/import expectations and sensitive-content handling.
  </action>
  <verify>
    <automated>JAVA_HOME=/home/coder/.local/jdks/jdk-17.0.18+8 ./android/gradlew -p android :app:assembleDebug</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
- Users can reach a dedicated backup screen, see exactly what export contains, and create only password-encrypted backups from settings.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire the full import flow with mode sheet, password gate, preview, category toggles, and completion report</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/BackupRestoreScreen.kt
android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsHomeScreen.kt
android/app/src/main/res/values/strings.xml
  </files>
  <action>
- Implement import UX exactly per locked decisions:
  - user picks a file through SAF `OpenDocument`
  - immediately show a bottom sheet asking `Overwrite` vs `Merge`
  - once mode is chosen, request the password before preview/apply
  - decrypt/analyze using `SettingsBackupRepository` and render a preview before any apply
  - require an explicit confirmation button before restoring

- Preview/report requirements:
  - show cross-version warnings without blocking known-category restore
  - show skipped invalid/unsupported items before apply
  - in merge mode, allow include/exclude toggles for whole categories only
  - display imported-values-win conflict behavior clearly in preview copy

- Apply/completion requirements:
  - call the repository apply API with the chosen mode and selected categories
  - stay in settings after apply; do not bounce to another screen/activity
  - show an inline completion summary with applied categories, skipped items, cleared selections, and repair checklist entries
  - update the settings-home banner/status line if the completion report says repair is still needed
  - provide repair CTAs that route to the affected settings area (provider selections, providers, prompts/app mappings) where possible
  - keep toast feedback in addition to the inline completion summary
  </action>
  <verify>
    <automated>JAVA_HOME=/home/coder/.local/jdks/jdk-17.0.18+8 ./android/gradlew -p android :app:assembleDebug</automated>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>
- Import now supports overwrite or merge with password gating, category preview/toggles, partial restore reporting, and post-restore repair guidance while keeping the user inside settings.
  </done>
</task>

</tasks>

<verification>
- `JAVA_HOME=/home/coder/.local/jdks/jdk-17.0.18+8 ./android/gradlew -p android :app:assembleDebug` passes.
- `JAVA_HOME=/home/coder/.local/jdks/jdk-17.0.18+8 ./android/gradlew -p android testDebugUnitTest` still passes after UI wiring.
</verification>

<success_criteria>
- Export is password-encrypted only and transparent about categories/sensitive contents.
- Import supports overwrite or merge, category selection, preview, and explicit confirmation.
- Cross-version warnings and partial-invalid reports are visible to users instead of blocking everything or failing silently.
- After restore, users remain in settings and get actionable repair guidance when selections or credentials need attention.
</success_criteria>

<output>
After completion, create `.planning/phases/06-settings-ux-import-export/06-04-SUMMARY.md`
</output>
