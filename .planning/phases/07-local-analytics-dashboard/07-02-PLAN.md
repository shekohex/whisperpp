---
phase: 07-local-analytics-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/analytics/DictationAnalyticsSession.kt
  - android/app/src/test/java/com/github/shekohex/whisperpp/analytics/DictationAnalyticsSessionTest.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
autonomous: true
requirements:
  - STATS-01
must_haves:
  truths:
    - One completed dictation run produces one successful analytics session with the final inserted text.
    - One cancelled dictation run produces one cancelled analytics session and no fake send metrics.
    - Streaming partials, retries, and command-mode flows never double-count analytics.
  artifacts:
    - path: android/app/src/main/java/com/github/shekohex/whisperpp/analytics/DictationAnalyticsSession.kt
      provides: Exactly-once terminal-outcome tracking for dictation analytics.
    - path: android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
      provides: Runtime hooks from dictation lifecycle into the analytics repository.
  key_links:
    - from: android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
      to: android/app/src/main/java/com/github/shekohex/whisperpp/analytics/AnalyticsRepository.kt
      via: completed/cancelled dictation outcome writes
      pattern: recordCompletedSession|recordCancelledSession
    - from: android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
      to: android/app/src/main/java/com/github/shekohex/whisperpp/analytics/DictationAnalyticsSession.kt
      via: per-run session state keyed by dictation lifecycle
      pattern: sessionId|finalize|cancel
---

<objective>
Capture analytics at the real dictation terminal outcomes so completed vs cancelled runs are accurate and never double-counted.

Purpose: turn the Phase 7 analytics repository into live app data without altering existing dictation behavior.
Output: exactly-once runtime analytics writes from the IME service.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-local-analytics-dashboard/07-CONTEXT.md
@.planning/phases/07-local-analytics-dashboard/07-RESEARCH.md
@.planning/phases/07-local-analytics-dashboard/07-01-SUMMARY.md
@android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/dictation/DictationController.kt

<interfaces>
From android/app/src/main/java/com/github/shekohex/whisperpp/dictation/DictationController.kt:
```kotlin
data class SendToken(
    val sessionId: Long,
    val focusKey: FocusKey?,
    val streamingPartialsEnabled: Boolean,
)

data class CapturedDictationSegment(
    val focusKey: FocusKey,
    val start: Int,
    val endAtInsert: Int,
    val rawText: String,
)

fun insertRawAndCaptureSegment(token: SendToken, rawText: String): CapturedDictationSegment?
fun replaceCapturedSegment(captured: CapturedDictationSegment, enhancedText: String): Boolean
```

Consume the plan-01 repository contract directly:
```kotlin
class AnalyticsRepository {
    suspend fun recordCompletedSession(durationMs: Long, rawText: String, finalInsertedText: String, recordedOn: LocalDate)
    suspend fun recordCancelledSession(durationMs: Long, recordedOn: LocalDate)
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add an exactly-once dictation analytics outcome helper</name>
  <files>android/app/src/main/java/com/github/shekohex/whisperpp/analytics/DictationAnalyticsSession.kt, android/app/src/test/java/com/github/shekohex/whisperpp/analytics/DictationAnalyticsSessionTest.kt</files>
  <behavior>
    - Test 1: one session can finalize once; duplicate finalize or cancel calls become no-ops.
    - Test 2: a session finalized with raw text and later replacement returns the enhanced final text without incrementing session count twice.
    - Test 3: a cancelled session never reports completed payload fields.
  </behavior>
  <action>Create a small helper that tracks one dictation run from start to terminal outcome. It must accept the dictation session id, raw transcript, optional final inserted text, cancel state, and recording duration; return exactly one write payload for the repository; and ignore partials, retries, duplicate callbacks, or command-mode activity. Keep it framework-free so the test class can pin the edge cases quickly.</action>
  <verify>
    <automated>./android/gradlew testDebugUnitTest --tests "com.github.shekohex.whisperpp.analytics.DictationAnalyticsSessionTest" -x lint</automated>
  </verify>
  <done>The helper proves completed vs cancelled outcomes are emitted once per dictation run, even across enhancement replacement and retry paths.</done>
</task>

<task type="auto">
  <name>task 2: wire WhisperInputService to record terminal dictation analytics</name>
  <files>android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt</files>
  <action>Instantiate `AnalyticsRepository` and the session helper inside `WhisperInputService`. Arm analytics when dictation actually starts, record cancelled sessions only on confirmed dictation cancellation, and record completed sessions only after the final inserted text is known: raw text for raw-only/fallback cases, enhanced text only when `replaceCapturedSegment` succeeds, raw text when focus changes or enhancement fails/skips. Use `recordingTimeMs` for duration, local calendar date for buckets, and keep command-mode flows out of analytics entirely. Do not count streaming partials, retries, clipboard copies caused by focus loss, or post-insert enhancement undo as new sessions.</action>
  <verify>
    <automated>./android/gradlew testDebugUnitTest --tests "com.github.shekohex.whisperpp.analytics.DictationAnalyticsSessionTest" -x lint &amp;&amp; ./android/gradlew assembleDebug</automated>
  </verify>
  <done>Real dictation runs now update analytics once per terminal outcome without regressing focus safety, undo behavior, or command-mode flows.</done>
</task>

</tasks>

<verification>
Run the helper unit test and a debug build. Manually inspect the wiring points in `WhisperInputService` to confirm only dictation terminal outcomes call the analytics repository.
</verification>

<success_criteria>
- Successful sends and cancellations are tracked separately.
- Final inserted text, not partial text, drives STATS-01 metrics.
- No dictation path writes analytics twice for one session.
</success_criteria>

<output>
After completion, create `.planning/phases/07-local-analytics-dashboard/07-02-SUMMARY.md`
</output>
