---
phase: 07-local-analytics-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/analytics/AnalyticsModels.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/analytics/AnalyticsFormatter.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/analytics/AnalyticsRepository.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/analytics/AnalyticsStore.kt
  - android/app/src/test/java/com/github/shekohex/whisperpp/analytics/AnalyticsFormatterTest.kt
  - android/app/src/test/java/com/github/shekohex/whisperpp/analytics/AnalyticsRepositoryTest.kt
  - android/app/src/test/java/com/github/shekohex/whisperpp/analytics/AnalyticsPrivacyTest.kt
  - android/app/src/main/res/xml/backup_rules.xml
  - android/app/src/main/res/xml/data_extraction_rules.xml
autonomous: true
requirements:
  - STATS-01
  - PRIV-06
must_haves:
  truths:
    - Analytics totals, time-saved math, and 7-day buckets load from a dedicated local store.
    - Reset clears lifetime totals and daily history in one operation.
    - Analytics storage stays outside normal backup and device-transfer flows.
  artifacts:
    - path: android/app/src/main/java/com/github/shekohex/whisperpp/analytics/AnalyticsRepository.kt
      provides: Local-only analytics persistence, aggregation, and reset API.
    - path: android/app/src/main/java/com/github/shekohex/whisperpp/analytics/AnalyticsFormatter.kt
      provides: Human-readable time-saved, WPM, and dashboard summary derivation.
    - path: android/app/src/main/res/xml/backup_rules.xml
      provides: Backup exclusion for the analytics store file.
    - path: android/app/src/main/res/xml/data_extraction_rules.xml
      provides: Cloud-backup and device-transfer exclusion for the analytics store file.
  key_links:
    - from: android/app/src/main/java/com/github/shekohex/whisperpp/analytics/AnalyticsRepository.kt
      to: android/app/src/main/java/com/github/shekohex/whisperpp/analytics/AnalyticsStore.kt
      via: dedicated analytics DataStore file
      pattern: analytics.*DataStore|PreferenceDataStoreFactory|preferencesDataStore
    - from: android/app/src/main/res/xml/backup_rules.xml
      to: android/app/src/main/res/xml/data_extraction_rules.xml
      via: matching exclusion path for analytics storage
      pattern: analytics
---

<objective>
Create the analytics foundation: immutable snapshot models, derived dashboard formatting, a dedicated local-only repository, and backup/device-transfer exclusions.

Purpose: satisfy STATS-01 + PRIV-06 before any runtime or settings UI wiring.
Output: tested analytics contracts plus explicit privacy-safe storage boundaries.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-local-analytics-dashboard/07-CONTEXT.md
@.planning/phases/07-local-analytics-dashboard/07-RESEARCH.md
@android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt
@android/app/src/main/res/xml/backup_rules.xml
@android/app/src/main/res/xml/data_extraction_rules.xml
@android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsBackupModels.kt

<interfaces>
From android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt:
```kotlin
val Context.dataStore: DataStore<Preferences> by preferencesDataStore(name = "settings")
```

From android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsBackupModels.kt:
```kotlin
val SETTINGS_BACKUP_CATEGORY_MANIFEST: List<SettingsBackupCategoryManifestEntry>
```

Create these new contracts in this plan so later plans can consume them without exploration:
```kotlin
data class AnalyticsSnapshot(...)
data class AnalyticsDayBucket(...)

class AnalyticsRepository {
    val snapshot: Flow<AnalyticsSnapshot>
    suspend fun recordCompletedSession(...)
    suspend fun recordCancelledSession(...)
    suspend fun resetAnalytics()
}

object AnalyticsFormatter {
    fun formatTimeSaved(...): String
    fun formatHeroSummary(...): String
    fun calculateWordsPerMinute(...): Int
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: define analytics snapshot contracts and formatter math</name>
  <files>android/app/src/main/java/com/github/shekohex/whisperpp/analytics/AnalyticsModels.kt, android/app/src/main/java/com/github/shekohex/whisperpp/analytics/AnalyticsFormatter.kt, android/app/src/test/java/com/github/shekohex/whisperpp/analytics/AnalyticsFormatterTest.kt</files>
  <behavior>
    - Test 1: time saved formats as minutes for small totals and hour+minute totals once the aggregate grows.
    - Test 2: words per minute uses final inserted words only and returns 0 for zero-duration or zero-word sessions.
    - Test 3: hero-summary copy stays typing-comparison-focused with a soft estimate caveat, not an alarming privacy headline.
  </behavior>
  <action>Create immutable analytics DTOs for lifetime totals, successful/cancelled session counts, raw/final word totals, inserted-character keystroke estimate, and fixed 7-day daily buckets. Implement `AnalyticsFormatter` with one centralized typing baseline constant, human-readable total formatting, motivational hero-summary copy, and helper math for WPM/time-saved/keystrokes. Keep display strings derived at read time only; do not persist formatted text.</action>
  <verify>
    <automated>./android/gradlew testDebugUnitTest --tests "com.github.shekohex.whisperpp.analytics.AnalyticsFormatterTest" -x lint</automated>
  </verify>
  <done>Analytics models compile, formatter behavior is pinned by tests, and the time-saved hero math matches the locked Phase 7 framing.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: implement the dedicated analytics repository and local aggregation rules</name>
  <files>android/app/src/main/java/com/github/shekohex/whisperpp/analytics/AnalyticsStore.kt, android/app/src/main/java/com/github/shekohex/whisperpp/analytics/AnalyticsRepository.kt, android/app/src/test/java/com/github/shekohex/whisperpp/analytics/AnalyticsRepositoryTest.kt</files>
  <behavior>
    - Test 1: recording one completed session updates lifetime minutes, successful-session count, raw/final word totals, WPM inputs, keystrokes saved, and the correct local-day bucket.
    - Test 2: recording one cancelled session increments only cancelled-session totals and does not inflate sent/final-word metrics.
    - Test 3: reset returns a zeroed snapshot and clears all 7 daily buckets/history in one write.
  </behavior>
  <action>Create a dedicated analytics Preferences DataStore file separate from `settings`. Implement repository APIs that expose a `Flow<AnalyticsSnapshot>` plus `recordCompletedSession`, `recordCancelledSession`, and `resetAnalytics`. Count words with `BreakIterator`, use final inserted text for WPM, use inserted-character count for keystrokes saved, keep lifetime totals since last reset plus rolling last-7-days buckets, and never store UI copy. Do not add analytics fields to `SettingsRepository`, backup payload models, or any shared settings JSON.</action>
  <verify>
    <automated>./android/gradlew testDebugUnitTest --tests "com.github.shekohex.whisperpp.analytics.AnalyticsRepositoryTest" -x lint</automated>
  </verify>
  <done>The repository can aggregate completed/cancelled sessions deterministically, emit a dashboard-ready snapshot, and fully zero itself on reset without touching existing settings persistence.</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: lock analytics behind a local-only backup boundary</name>
  <files>android/app/src/main/res/xml/backup_rules.xml, android/app/src/main/res/xml/data_extraction_rules.xml, android/app/src/test/java/com/github/shekohex/whisperpp/analytics/AnalyticsPrivacyTest.kt</files>
  <behavior>
    - Test 1: privacy verification fails if the analytics store path is missing from either backup exclusion file.
    - Test 2: privacy verification fails if analytics categories appear in the Phase 6 backup manifest/export surface.
  </behavior>
  <action>Add explicit excludes for the analytics store file in both `backup_rules.xml` and `data_extraction_rules.xml` (cloud-backup and device-transfer sections). Write `AnalyticsPrivacyTest` to assert the analytics storage path is excluded everywhere and that Phase 6 backup metadata still omits analytics categories entirely. Keep analytics local-only: no import/export category, no network payload, no log emission.</action>
  <verify>
    <automated>./android/gradlew testDebugUnitTest --tests "com.github.shekohex.whisperpp.analytics.AnalyticsPrivacyTest" -x lint</automated>
  </verify>
  <done>Analytics storage is technically isolated from backups/transfers, and tests will fail if a later change reintroduces analytics into backup/export surfaces.</done>
</task>

</tasks>

<verification>
Run the three targeted analytics unit-test classes. Confirm the analytics store is a separate file from `settings` and both XML rule files exclude that path.
</verification>

<success_criteria>
- A dedicated analytics repository exists with tested completed/cancelled aggregation.
- Time-saved, WPM, and hero-summary math are centralized and deterministic.
- Reset clears totals and history.
- Analytics are excluded from backup/export by default.
</success_criteria>

<output>
After completion, create `.planning/phases/07-local-analytics-dashboard/07-01-SUMMARY.md`
</output>
