---
phase: 07-local-analytics-dashboard
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/AnalyticsDashboardCard.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/AnalyticsDashboardScreen.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsHomeScreen.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsHelp.kt
  - android/app/src/main/res/values/strings.xml
  - android/app/src/androidTest/java/com/github/shekohex/whisperpp/ui/settings/AnalyticsDashboardUiTest.kt
autonomous: true
requirements:
  - STATS-02
  - PRIV-06
must_haves:
  truths:
    - Settings home shows analytics as the first normal section, led by time saved with a visible 7-day preview.
    - Tapping the home card opens a dedicated analytics screen with lifetime totals, sent/cancelled breakdowns, raw/final word breakdowns, and a visible empty trend scaffold.
    - Reset uses a destructive confirmation dialog and immediately zeroes the dashboard in place.
  artifacts:
    - path: android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/AnalyticsDashboardCard.kt
      provides: Home-screen mini dashboard card with hero metric and compact trend preview.
    - path: android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/AnalyticsDashboardScreen.kt
      provides: Dedicated analytics destination and reset flow.
    - path: android/app/src/androidTest/java/com/github/shekohex/whisperpp/ui/settings/AnalyticsDashboardUiTest.kt
      provides: Instrumented coverage for analytics route, reset, and empty-state rendering.
  key_links:
    - from: android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsHomeScreen.kt
      to: android/app/src/main/java/com/github/shekohex/whisperpp/analytics/AnalyticsRepository.kt
      via: snapshot flow collection and prop wiring into AnalyticsDashboardCard
      pattern: AnalyticsRepository|snapshot|collectAsState|AnalyticsDashboardCard
    - from: android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsHomeScreen.kt
      to: android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/AnalyticsDashboardScreen.kt
      via: settings navigation route
      pattern: SettingsScreen\.Analytics|navigate
    - from: android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/AnalyticsDashboardScreen.kt
      to: android/app/src/main/java/com/github/shekohex/whisperpp/analytics/AnalyticsRepository.kt
      via: repository flow collection and reset call
      pattern: collectAsState|resetAnalytics
---

<objective>
Ship the Phase 7 settings surfaces: a mini analytics dashboard on home plus a dedicated analytics screen with trends and reset.

Purpose: satisfy STATS-02 while honoring the locked dashboard layout, hero metric, and reset behavior.
Output: analytics settings UI, route/help wiring, and instrumentation coverage.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-local-analytics-dashboard/07-CONTEXT.md
@.planning/phases/07-local-analytics-dashboard/07-RESEARCH.md
@.planning/phases/07-local-analytics-dashboard/07-VALIDATION.md
@.planning/phases/07-local-analytics-dashboard/07-01-SUMMARY.md
@android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsHomeScreen.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsHelp.kt

<interfaces>
From android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt:
```kotlin
sealed class SettingsScreen(val route: String) {
    object Main : SettingsScreen("main")
    object BackupRestore : SettingsScreen("backup_restore")
}
```

Consume the plan-01 repository contract directly inside settings UI:
```kotlin
class AnalyticsRepository {
    val snapshot: Flow<AnalyticsSnapshot>
    suspend fun resetAnalytics()
}

data class AnalyticsSnapshot(
    val successfulSessions: Int,
    val cancelledSessions: Int,
    val rawWords: Int,
    val finalInsertedWords: Int,
    val timeSavedSeconds: Long,
    val last7Days: List<AnalyticsDayBucket>,
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: build reusable analytics dashboard composables and copy</name>
  <files>android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/AnalyticsDashboardCard.kt, android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/AnalyticsDashboardScreen.kt, android/app/src/main/res/values/strings.xml</files>
  <action>Create a home mini-dashboard card and a dedicated analytics screen in the `ui.settings` package. Match the locked Phase 7 decisions: hero metric is time saved, home card is a two-row mini dashboard with a small 7-day visual preview, and the 2-3 supporting home metrics must explicitly surface the roadmap-visible lifetime totals for dictation minutes, words per minute, and keystrokes saved (choose any 2 on the home card, but the dedicated screen must render all 3 as named stats). The dedicated screen also keeps sent/cancelled and raw/final word breakdowns, and the 7-day trend visualizes time saved. Use a fixed Compose visual (bars/points/rows), not a new chart library unless compatibility is proven immediately. Keep the trend area visible with an empty scaffold when history is sparse, and use motivational typing-comparison copy rather than privacy-heavy empty-state messaging.</action>
  <verify>
    <automated>./android/gradlew assembleDebug</automated>
  </verify>
  <done>The new analytics UI primitives render hero, supporting metrics, trend preview, breakdown sections, empty scaffolds, and reset copy without needing any additional chart dependency.</done>
</task>

<task type="auto">
  <name>task 2: wire analytics into settings home, navigation, help, and reset flow</name>
  <files>android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsHomeScreen.kt, android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt, android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsHelp.kt</files>
  <action>Add `SettingsScreen.Analytics` to the route whitelist and navigation graph, then collect `AnalyticsRepository.snapshot` as live state in the settings UI layer and pass that snapshot into the home card and analytics destination instead of static preview/demo values. Insert the analytics mini dashboard as the first normal section on `SettingsHomeScreen` directly below the setup banner and above every existing grouped section. Tapping the card must open the dedicated analytics screen. Keep the setup banner behavior unchanged. On the analytics screen, add a destructive confirmation dialog for reset, call `resetAnalytics()`, remain on the same route, and re-render the zeroed dashboard immediately after confirmation. Add route-aware help content for analytics and do not move sent/cancelled breakdowns onto the home card.</action>
  <verify>
    <automated>./android/gradlew assembleDebug</automated>
  </verify>
  <done>Settings home order matches the locked layout, navigation reaches the analytics destination cleanly, help text exists, and reset stays in-place while zeroing the dashboard.</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: add instrumentation coverage for analytics dashboard behavior</name>
  <files>android/app/src/androidTest/java/com/github/shekohex/whisperpp/ui/settings/AnalyticsDashboardUiTest.kt</files>
  <behavior>
    - Test 1: settings home shows the analytics card before the existing grouped settings sections and opens the analytics route when tapped.
    - Test 2: the analytics screen shows an empty trend scaffold when there is no history, not a hidden section.
    - Test 3: the analytics screen renders named lifetime totals for dictation minutes, words per minute, and keystrokes saved.
    - Test 4: reset confirmation zeroes the hero/breakdowns and keeps the user on the analytics screen.
  </behavior>
  <action>Create an Android instrumentation test that renders the settings navigation with analytics state injected, asserts the home-card ordering/navigation, verifies the empty trend scaffold, explicitly checks that dictation minutes, words per minute, and keystrokes saved are visible as named lifetime totals on the analytics screen, and confirms destructive reset behavior leaves the analytics screen visible with zeroed content. Add semantics/test tags only where needed for stable assertions; keep production UI unchanged otherwise.</action>
  <verify>
    <automated>./android/gradlew connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.github.shekohex.whisperpp.ui.settings.AnalyticsDashboardUiTest</automated>
  </verify>
  <done>The analytics UI has automated coverage for home placement, route navigation, explicit lifetime totals (dictation minutes/WPM/keystrokes saved), empty-state rendering, and in-place reset semantics.</done>
</task>

</tasks>

<verification>
Build debug, then run the targeted analytics instrumentation test. Confirm the home-card ordering, dedicated analytics route, empty trend scaffold, and reset dialog behavior are all covered.
</verification>

<success_criteria>
- Home settings surface now leads with analytics after any setup banner.
- Dedicated analytics screen looks like a mini Digital Wellbeing-style dashboard with real trend visuals.
- Dedicated analytics stats are explicit and viewable: lifetime dictation minutes, words per minute, and keystrokes saved.
- Reset is destructive, in-place, and immediate.
- Analytics remain local-only and never enter import/export or network flows.
</success_criteria>

<output>
After completion, create `.planning/phases/07-local-analytics-dashboard/07-03-SUMMARY.md`
</output>
