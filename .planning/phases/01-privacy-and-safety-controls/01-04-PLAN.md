---
phase: 01-privacy-and-safety-controls
plan: 04
type: execute
wave: 4
depends_on:
  - "01-03"
files_modified:
  - android/app/src/main/AndroidManifest.xml
  - android/app/src/main/java/com/github/shekohex/whisperpp/privacy/SendPolicyRepository.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreen.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
autonomous: true

must_haves:
  truths:
    - "User can block all external sending for a specific app"
    - "When blocked, Whisper++ refuses to send and explains the block"
    - "Blocking is keyed by EditorInfo.packageName"
  artifacts:
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/privacy/SendPolicyRepository.kt"
      provides: "Persisted per-app allow/block policy"
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreen.kt"
      provides: "Per-app policy UI with search and toggles"
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "Package visibility queries for launcher enumeration"
  key_links:
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt"
      to: "SendPolicyRepository"
      via: "check current app packageName"
      pattern: "packageName|SendPolicyRepository"
---

<objective>
Add per-app send policy (default allow) with a searchable installed-apps settings UI, enforce it in the IME send gate, and clearly explain blocks.

Purpose: Meet PRIV-04 (per-app external sending control).
Output: Per-app policy storage + settings UI + IME enforcement + blocked UX.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-privacy-and-safety-controls/01-CONTEXT.md
@.planning/phases/01-privacy-and-safety-controls/01-RESEARCH.md

@android/app/src/main/AndroidManifest.xml
@android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement per-app send policy persistence keyed by package name</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/privacy/SendPolicyRepository.kt
android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt
  </files>
  <action>
- Add a preference key (string JSON) for per-app policy, e.g. `PER_APP_SEND_POLICY_JSON`.
- Implement `SendPolicyRepository(dataStore)`:
  - Stores `Map<String, Boolean>` of `packageName -> blocked` (true=blocked).
  - APIs:
    - `isBlockedFlow(packageName: String?): Flow<Boolean>` (null -> false)
    - `setBlocked(packageName: String, blocked: Boolean)`
    - `getAllRulesFlow(): Flow<Map<String, Boolean>>`

- Storage constraints:
  - Default allow when no rule exists.
  - Never log package list beyond what’s needed for UI.
  </action>
  <verify>
JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew :app:assembleDebug
  </verify>
  <done>
- Per-app send policy can be persisted and read as a Flow.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Privacy & Safety settings screen with installed-apps list + search + block toggles</name>
  <files>
android/app/src/main/AndroidManifest.xml
android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreen.kt
  </files>
  <action>
- Manifest package visibility:
  - Add `<queries>` allowing launcher enumeration:
    - intent action MAIN + category LAUNCHER.

- Create `PrivacySafetyScreen.kt`:
  - UI sections:
    - Per-app send policy (search field + list).
  - List data source:
    - Use `packageManager.queryIntentActivities(Intent(ACTION_MAIN).addCategory(CATEGORY_LAUNCHER), 0)`.
    - Distinct by packageName.
    - Show app label + package name.
  - Each row includes a single toggle: “Allow external sending” (ON by default). When OFF, store blocked=true.
  - Add a fallback “Add rule by package name” (text field + add) so non-launchable packages can still be blocked.

- Navigation wiring:
  - Add a new `SettingsScreen` route (e.g., `PrivacySafety`).
  - Add an entry point from `MainSettingsScreen`.
  - Deep-link wiring (for blocked sheets / IME):
    - Add support for opening Settings directly to the Privacy & Safety route using a simple destination identifier (from 01-02), e.g. `"privacy_safety"`.
    - Ensure the Settings entry point in `MainActivity` accepts an optional destination extra/arg and navigates to `PrivacySafety` when present.
    - If the destination is unknown/missing, fall back to the main settings screen.
  </action>
  <verify>
JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew :app:assembleDebug
  </verify>
  <done>
- Users can search apps and toggle allow/block.
- Policy persists and is reflected when returning to the screen.
  </done>
</task>

<task type="auto">
  <name>Task 3: Enforce per-app policy in IME send gate + blocked explanation UX</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
  </files>
  <action>
- In `WhisperInputService`, incorporate per-app policy into the existing send gate (single entry point for all external sending: dictation + Smart Fix + future command mode):
  - Determine current target package via `currentInputEditorInfo?.packageName`.
  - If blocked:
    - disable starting dictation/transcription/smart-fix
    - if currently active, stop immediately (recorder stop + transcriber stop + smartFixer.cancel())
    - show a short notice toast like “External sending blocked for this app”.

- Keyboard UX:
  - Reuse the existing blocked sheet mechanism from 01-02.
  - When blocked due to per-app policy, sheet must:
    - state it’s blocked by an app rule
    - offer a button to open “Privacy & Safety” settings (must use the deep-link destination so it lands on Privacy & Safety, not Settings main).
  - Ensure no proactive banner.
  </action>
  <verify>
JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew :app:assembleDebug
  </verify>
  <done>
- When per-app policy is blocked, Whisper++ refuses to send and explains why on tap.
- Stop-immediately behavior triggers when policy becomes blocked for current editor.
  </done>
</task>

</tasks>

<verification>
- Build: `JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew assembleDebug`
</verification>

<success_criteria>
- PRIV-04 satisfied: per-app block UI exists and is enforced by packageName.
</success_criteria>

<output>
After completion, create `.planning/phases/01-privacy-and-safety-controls/01-04-SUMMARY.md`
</output>
