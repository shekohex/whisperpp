---
phase: 01-privacy-and-safety-controls
plan: 04
type: execute
wave: 3
depends_on:
  - "01-02"
  - "01-03"
files_modified:
  - android/app/src/main/AndroidManifest.xml
  - android/app/src/main/java/com/github/shekohex/whisperpp/privacy/SendPolicyRepository.kt
  - android/app/src/test/java/com/github/shekohex/whisperpp/privacy/SendPolicyRepositoryTest.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreen.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
autonomous: true
requirements:
  - PRIV-04
must_haves:
  truths:
    - Users can search installed apps and block external sending with a single allow/block toggle.
    - Apps without a rule remain allowed by default.
    - When the current app is blocked, Whisper++ refuses to send and explains that the block came from that app's policy rule.
  artifacts:
    - path: android/app/src/main/java/com/github/shekohex/whisperpp/privacy/SendPolicyRepository.kt
      provides: Persisted packageName -> blocked rules with default-allow semantics.
    - path: android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreen.kt
      provides: Searchable app-policy management UI with package-focused deep-link support.
    - path: android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
      provides: App-policy enforcement inside the IME send gate.
  key_links:
    - from: android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
      to: android/app/src/main/java/com/github/shekohex/whisperpp/privacy/SendPolicyRepository.kt
      via: current target packageName check
      pattern: SendPolicyRepository|packageName
    - from: android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt
      to: android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
      via: privacy_safety destination extra plus blocked-package focus state
      pattern: EXTRA_SETTINGS_DESTINATION|EXTRA_SETTINGS_PACKAGE|resolveStartRoute
---

<objective>
Ship per-app external-send control through settings and IME enforcement.

Purpose: satisfy PRIV-04 with default-allow semantics, clear routing into Privacy & Safety settings, and block-specific explanation copy.
Output: send-policy persistence, searchable installed-app list UI, package-aware deep-linkable Privacy & Safety routing, and IME enforcement by package name.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-privacy-and-safety-controls/01-CONTEXT.md
@.planning/phases/01-privacy-and-safety-controls/01-RESEARCH.md
@android/app/src/main/AndroidManifest.xml
@android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt

<interfaces>
Current route and policy contracts:

```kotlin
sealed class SettingsScreen(val route: String) {
    object PrivacySafety : SettingsScreen("privacy_safety")
}
```

```kotlin
class SendPolicyRepository(private val dataStore: DataStore<Preferences>) {
    fun isBlockedFlow(packageName: String?): Flow<Boolean>
    suspend fun setBlocked(packageName: String, blocked: Boolean)
    fun getAllRulesFlow(): Flow<Map<String, Boolean>>
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add per-app send-policy persistence with default-allow tests</name>
  <files>android/app/src/main/java/com/github/shekohex/whisperpp/privacy/SendPolicyRepository.kt, android/app/src/test/java/com/github/shekohex/whisperpp/privacy/SendPolicyRepositoryTest.kt</files>
  <behavior>
    - Test 1: packages without a stored rule resolve to allowed (`false` for blocked).
    - Test 2: setting a rule to blocked persists `packageName -> true` and clearing it removes the rule instead of storing `false`.
    - Test 3: malformed JSON returns an empty rule map instead of crashing the UI or IME.
  </behavior>
  <action>Write the failing repository tests first, then implement `SendPolicyRepository` around a JSON-backed `packageName -> blocked` map. Keep default-allow semantics explicit by removing false entries instead of persisting them. Do not log the installed-app list or rule set.</action>
  <verify>
    <automated>./android/gradlew testDebugUnitTest --tests "com.github.shekohex.whisperpp.privacy.SendPolicyRepositoryTest"</automated>
  </verify>
  <done>Policy persistence is deterministic, default-allow is preserved, and malformed state cannot break the feature.</done>
</task>

<task type="auto">
  <name>task 2: add the Privacy and Safety route with searchable app blocking UI</name>
  <files>android/app/src/main/AndroidManifest.xml, android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt, android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt, android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreen.kt</files>
  <action>Add launcher-app package visibility via `<queries>` in the manifest, then create the dedicated `privacy_safety` settings route and entry point. `PrivacySafetyScreen` must show a searchable installed-app list, the app label, package name details, and one allow/block toggle per app. Keep the policy model as a single block-all-external-sending switch, not separate audio/text toggles. Route IME deep links through `MainActivity.EXTRA_SETTINGS_DESTINATION` plus a package-focused argument/state carrier such as `EXTRA_SETTINGS_PACKAGE` so unknown values fall back safely to settings home while `privacy_safety` lands directly on this screen and can preselect, filter, or highlight the blocked app's rule.</action>
  <verify>
    <automated>./android/gradlew assembleDebug</automated>
  </verify>
  <done>Privacy & Safety is a first-class settings destination, installed apps are searchable, and deep links can open the screen already focused on one blocked app rule.</done>
</task>

<task type="auto">
  <name>task 3: enforce app-policy blocks inside the unified IME send gate</name>
  <files>android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt, android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt</files>
  <action>Extend the existing external-send gate so secure-field and per-app policy checks resolve through one decision function. Use `currentInputEditorInfo?.packageName` as the source of truth. When the current app is blocked, disable dictation and command actions, stop any active recording/transcription/enhancement work immediately, and reuse the blocked-action explanation path with app-policy-specific copy plus a deep link that passes the blocked package name so Privacy & Safety opens focused on that app's rule instead of the generic screen.</action>
  <verify>
    <automated>./android/gradlew assembleDebug</automated>
  </verify>
  <done>Blocked apps cannot send audio or text externally, the lock affordance stays visible, and the explanation opens Privacy & Safety focused on the blocked app's rule.</done>
</task>

</tasks>

<verification>
- `./android/gradlew testDebugUnitTest --tests "com.github.shekohex.whisperpp.privacy.SendPolicyRepositoryTest"`
- `./android/gradlew assembleDebug`
</verification>

<success_criteria>
- PRIV-04 is satisfied with default-allow semantics and package-name-based enforcement.
- Privacy & Safety deep links work from IME and from settings navigation alike, including package-focused routing back to the blocked app's rule.
</success_criteria>

<output>
After completion, create `.planning/phases/01-privacy-and-safety-controls/01-04-SUMMARY.md`
</output>
