---
phase: 01-privacy-and-safety-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/WhisperTranscriber.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/SmartFixer.kt
  - android/app/src/test/java/com/github/shekohex/whisperpp/WhisperTranscriberSafetyTest.kt
  - android/app/src/test/java/com/github/shekohex/whisperpp/SmartFixerSafetyTest.kt
autonomous: true
requirements:
  - PRIV-03
must_haves:
  truths:
    - User never gets raw provider payloads in error messages.
    - Default networking diagnostics never log auth headers or user audio/text bodies.
    - Cancelling dictation or enhancement cancels the in-flight OkHttp call immediately.
  artifacts:
    - path: android/app/src/main/java/com/github/shekohex/whisperpp/WhisperTranscriber.kt
      provides: Cancelable transcription client with safe error surfaces.
    - path: android/app/src/main/java/com/github/shekohex/whisperpp/SmartFixer.kt
      provides: Cancelable enhancement client with redacted diagnostics.
    - path: android/app/src/test/java/com/github/shekohex/whisperpp/WhisperTranscriberSafetyTest.kt
      provides: Regression coverage for transcription logging and cancellation rules.
  key_links:
    - from: android/app/src/main/java/com/github/shekohex/whisperpp/WhisperTranscriber.kt
      to: okhttp3.Call
      via: in-flight call ownership and stop()
      pattern: inFlightCall|call\.cancel\(
    - from: android/app/src/main/java/com/github/shekohex/whisperpp/SmartFixer.kt
      to: okhttp3.Call
      via: in-flight call ownership and cancel()
      pattern: inFlightCall|call\.cancel\(
---

<objective>
Build the networking safety foundation for Phase 01.

Purpose: enforce PRIV-03 before any IME privacy gate relies on cancellation or diagnostics behavior.
Output: redacted-by-default transcription and enhancement clients, plus regression tests for safe logging and cancellation.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-privacy-and-safety-controls/01-CONTEXT.md
@.planning/phases/01-privacy-and-safety-controls/01-RESEARCH.md
@android/app/src/main/java/com/github/shekohex/whisperpp/WhisperTranscriber.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/SmartFixer.kt

<interfaces>
Current integration points to preserve:

```kotlin
class WhisperTranscriber {
    fun startAsync(...)
    fun stop()
}

class SmartFixer {
    suspend fun fix(...): String
}
```

Both clients already use OkHttp. Keep auth behavior provider-specific, but move diagnostics to NONE by default and HEADERS-only when verbose logging is enabled.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: harden transcription networking with regression coverage</name>
  <files>android/app/src/main/java/com/github/shekohex/whisperpp/WhisperTranscriber.kt, android/app/src/test/java/com/github/shekohex/whisperpp/WhisperTranscriberSafetyTest.kt</files>
  <behavior>
    - Test 1: default logging mode is NONE; enabling verbose logging only raises the client to HEADERS and still redacts Authorization.
    - Test 2: non-2xx failures surface only provider name, HTTP status, and endpoint host; raw response bodies never appear.
    - Test 3: stop() cancels the tracked OkHttp Call and cancelled IO paths return silently without invoking the unsafe error path.
  </behavior>
  <action>Write the failing safety tests first, then update `WhisperTranscriber` to own its active `Call`, cancel it from `stop()`, suppress cancel-triggered `IOException`s, and derive safe error copy from provider name + status code + host only. Keep request/response body logging disabled in all modes; verbose diagnostics may log only redacted headers.</action>
  <verify>
    <automated>./android/gradlew testDebugUnitTest --tests "com.github.shekohex.whisperpp.WhisperTranscriberSafetyTest"</automated>
  </verify>
  <done>Transcription cancellation reaches `Call.cancel()`, cancelled uploads do not leak scary errors, and the regression test proves no payload/body text reaches logs or UI errors.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: harden Smart Fix networking with regression coverage</name>
  <files>android/app/src/main/java/com/github/shekohex/whisperpp/SmartFixer.kt, android/app/src/test/java/com/github/shekohex/whisperpp/SmartFixerSafetyTest.kt</files>
  <behavior>
    - Test 1: Smart Fix defaults to no HTTP logging and verbose mode remains HEADERS-only with `Authorization` and `x-goog-api-key` redacted.
    - Test 2: error handling never logs or surfaces raw model response bodies.
    - Test 3: cancel() aborts the tracked OkHttp Call and cancelled enhancement requests end as benign cancellations.
  </behavior>
  <action>Write the failing Smart Fix safety tests first, then update `SmartFixer` to mirror the same privacy rules as transcription: explicit in-flight `Call` tracking, a public `cancel()` used by later IME gates, HEADERS-only verbose diagnostics, and safe provider/status/host error text with no body logging.</action>
  <verify>
    <automated>./android/gradlew testDebugUnitTest --tests "com.github.shekohex.whisperpp.SmartFixerSafetyTest"</automated>
  </verify>
  <done>Smart Fix is cancelable, verbose diagnostics remain redacted, and safety regressions are covered by unit tests.</done>
</task>

</tasks>

<verification>
- `./android/gradlew testDebugUnitTest --tests "com.github.shekohex.whisperpp.WhisperTranscriberSafetyTest" --tests "com.github.shekohex.whisperpp.SmartFixerSafetyTest"`
- `./android/gradlew assembleDebug`
</verification>

<success_criteria>
- PRIV-03 is satisfied for both network clients before any UI/privacy flow depends on them.
- Cancellation is real network cancellation, not coroutine-only cancellation.
</success_criteria>

<output>
After completion, create `.planning/phases/01-privacy-and-safety-controls/01-01-SUMMARY.md`
</output>
