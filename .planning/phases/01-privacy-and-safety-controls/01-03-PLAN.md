---
phase: 01-privacy-and-safety-controls
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/privacy/SecretsStore.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/data/ProviderModels.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt
  - android/app/src/test/java/com/github/shekohex/whisperpp/data/ProviderSecretsMigrationTest.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
autonomous: true
requirements:
  - PRIV-02
must_haves:
  truths:
    - Provider API keys are not serialized into settings JSON or exports.
    - Existing plaintext keys migrate into protected storage before provider deserialization drops them.
    - Provider edit UI shows only key-set state and last four characters; runtime requests still authenticate after restart.
  artifacts:
    - path: android/app/src/main/java/com/github/shekohex/whisperpp/privacy/SecretsStore.kt
      provides: Keystore-backed provider secret storage.
    - path: android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt
      provides: sanitize-then-flag migration and secret-free export/import behavior.
    - path: android/app/src/test/java/com/github/shekohex/whisperpp/data/ProviderSecretsMigrationTest.kt
      provides: Regression coverage for plaintext key migration ordering.
  key_links:
    - from: android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt
      to: android/app/src/main/java/com/github/shekohex/whisperpp/privacy/SecretsStore.kt
      via: raw JSON migration before provider reads
      pattern: PROVIDER_API_KEY_MIGRATION_DONE|setProviderApiKey
    - from: android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
      to: android/app/src/main/java/com/github/shekohex/whisperpp/privacy/SecretsStore.kt
      via: runtime provider.copy(apiKey = ...)
      pattern: copy\(apiKey =|getProviderApiKey
---

<objective>
Move provider credentials out of plaintext persistence and into protected runtime-only storage.

Purpose: satisfy PRIV-02 without losing existing user keys during migration.
Output: Keystore-backed secret storage, sanitized provider persistence/export, masked provider editing, and runtime-only API key injection.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-privacy-and-safety-controls/01-CONTEXT.md
@.planning/phases/01-privacy-and-safety-controls/01-RESEARCH.md
@android/app/src/main/java/com/github/shekohex/whisperpp/data/ProviderModels.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt

<interfaces>
Existing contracts to keep stable:

```kotlin
class SecretsStore(context: Context) {
    fun setProviderApiKey(providerId: String, apiKey: String)
    fun getProviderApiKey(providerId: String): String?
    fun clearProviderApiKey(providerId: String)
    fun getProviderApiKeyLast4(providerId: String): String?
}
```

Provider runtime must still accept `provider.copy(apiKey = key)` while persistence stops serializing that field.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>task 1: add keystore-backed secret storage and runtime-only provider secret contract</name>
  <files>android/app/src/main/java/com/github/shekohex/whisperpp/privacy/SecretsStore.kt, android/app/src/main/java/com/github/shekohex/whisperpp/data/ProviderModels.kt</files>
  <action>Implement `SecretsStore` with Android Keystore AES/GCM encryption and app-private storage keyed by provider ID. Do not add any third-party crypto dependency. Update `ServiceProvider` so `apiKey` remains available as runtime data but is excluded from Gson persistence, export payloads, and any debug output. Keep the runtime shape compatible with later network calls by using `provider.copy(apiKey = loadedKey)` at send time instead of reworking every call site.</action>
  <verify>
    <automated>./android/gradlew assembleDebug</automated>
  </verify>
  <done>Secrets have a protected storage home, and provider models can still carry runtime API keys without serializing them.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: migrate plaintext keys, sanitize import/export, and mask provider editing</name>
  <files>android/app/src/main/java/com/github/shekohex/whisperpp/data/SettingsRepository.kt, android/app/src/test/java/com/github/shekohex/whisperpp/data/ProviderSecretsMigrationTest.kt, android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/SettingsScreen.kt, android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt</files>
  <behavior>
    - Test 1: raw `providers_json` entries with `apiKey` move into `SecretsStore`, then write back sanitized JSON before setting `PROVIDER_API_KEY_MIGRATION_DONE`.
    - Test 2: parse failures or unknown JSON shapes do not set the migration flag and do not destroy the stored JSON.
    - Test 3: exported and imported settings ignore API keys entirely, while runtime request setup still injects keys from `SecretsStore` only at call time.
  </behavior>
  <action>Write the failing migration tests first, then update `SettingsRepository` to parse the raw JSON tree, extract `id`/`apiKey`, store secrets, remove the `apiKey` property, write sanitized JSON back, and only then set the migration guard. Call this migration before any provider flow is consumed from both `SettingsNavigation` and `WhisperInputService.onCreate()`. In provider editing UI, never prefill the key value; show only `Key set` plus last four characters and support replace/clear without copy/export. In IME runtime, load the selected provider key from `SecretsStore` immediately before transcription or enhancement requests.</action>
  <verify>
    <automated>./android/gradlew testDebugUnitTest --tests "com.github.shekohex.whisperpp.data.ProviderSecretsMigrationTest"</automated>
  </verify>
  <done>No provider settings JSON or export payload contains plaintext API keys, migration preserves existing secrets, and runtime calls continue authenticating after restart.</done>
</task>

</tasks>

<verification>
- `./android/gradlew testDebugUnitTest --tests "com.github.shekohex.whisperpp.data.ProviderSecretsMigrationTest"`
- `./android/gradlew assembleDebug`
</verification>

<success_criteria>
- PRIV-02 is satisfied without data loss for existing users.
- Provider credentials are visible only as masked state in UI and never as serialized plaintext.
</success_criteria>

<output>
After completion, create `.planning/phases/01-privacy-and-safety-controls/01-03-SUMMARY.md`
</output>
