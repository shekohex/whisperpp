---
phase: 01-privacy-and-safety-controls
plan: 05
type: execute
wave: 5
depends_on:
  - "01-04"
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatter.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreen.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/WhisperTranscriber.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/SmartFixer.kt
autonomous: true

must_haves:
  truths:
    - "UI discloses what data is sent (audio/text) and which provider endpoints receive it"
    - "A one-time first-use disclosure appears before sending (mode-specific)"
    - "Verbose networking logs can be enabled but remain redacted (no auth headers, no payload bodies)"
  artifacts:
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatter.kt"
      provides: "Mode-specific disclosure strings derived from actual configuration"
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreen.kt"
      provides: "Privacy & Safety disclosure UI + diagnostics toggles"
  key_links:
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt"
      to: "PrivacyDisclosureFormatter"
      via: "first-use disclosure gating"
      pattern: "Disclosure|PrivacyDisclosureFormatter"
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt"
      to: "WhisperTranscriber.setNetworkLoggingEnabled"
      via: "verbose logs toggle"
      pattern: "setNetworkLoggingEnabled"
---

<objective>
Add explicit disclosure (settings + first-use) of what data is sent and to which endpoints, plus a redacted verbose-network-logs toggle that never logs payloads.

Purpose: Meet PRIV-05 and finalize PRIV-03 diagnostics boundary.
Output: Privacy disclosures in settings + first-use modal gating + redacted verbose logging toggle.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-privacy-and-safety-controls/01-CONTEXT.md
@.planning/phases/01-privacy-and-safety-controls/01-RESEARCH.md

@android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreen.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/WhisperTranscriber.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/SmartFixer.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add disclosure formatter derived from real endpoints + context usage</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatter.kt
  </files>
  <action>
- Implement `PrivacyDisclosureFormatter` as pure functions (no UI):
  - Inputs: provider endpoint(s), provider type, selected model id, whether “Use Context” is enabled.
  - Outputs: mode-specific disclosure data:
    - Dictation (audio): base domain + endpoint path(s) used for transcription; describe audio upload.
    - Enhancement (text / Smart Fix): base domain + endpoint path(s) used; describe transcript/context text sending.
    - Command mode (text): include placeholders even if feature isn’t built yet.

- Endpoint handling:
  - Use OkHttp `HttpUrl` parsing.
  - Display only base URL (scheme+host+optional port) and endpoint path (no query).
  - Special case Gemini SmartFix path computation: disclose base + `/models/{model}:generateContent`.
  </action>
  <verify>
JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew :app:assembleDebug
  </verify>
  <done>
- Formatter can produce disclosure strings for dictation and Smart Fix based on current config.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend Privacy & Safety screen with disclosures + verbose logs toggle (still redacted)</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt
android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreen.kt
  </files>
  <action>
- Add DataStore keys:
  - `VERBOSE_NETWORK_LOGS_ENABLED` (boolean, default false).
  - Disclosure “first-use shown” flags (booleans):
    - `DISCLOSURE_SHOWN_DICTATION_AUDIO`
    - `DISCLOSURE_SHOWN_ENHANCEMENT_TEXT`
    - `DISCLOSURE_SHOWN_COMMAND_TEXT`

- Privacy & Safety screen additions:
  - Add a “Disclosure” section that renders:
    - What data is sent (audio/text)
    - Which provider endpoint(s) receive it (domain + path)
    - Note optional context text if enabled
  - Add a “Diagnostics” section:
    - Toggle for verbose networking logs
    - Copy must be explicit: even when enabled, logs are redacted and payload bodies are never logged
    - Provide “Reset first-use disclosures” action (clears the disclosure-shown flags)
  </action>
  <verify>
JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew :app:assembleDebug
  </verify>
  <done>
- Privacy & Safety screen shows mode-specific endpoint disclosures.
- Verbose networking logs toggle is persisted.
- First-use disclosure flags can be reset.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add first-use disclosure gating before sending + wire verbose log toggle to network clients</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
android/app/src/main/java/com/github/shekohex/whisperpp/WhisperTranscriber.kt
android/app/src/main/java/com/github/shekohex/whisperpp/SmartFixer.kt
  </files>
  <action>
- Verbose logs wiring:
  - In `WhisperInputService`, read `VERBOSE_NETWORK_LOGS_ENABLED` and call:
    - `whisperTranscriber.setNetworkLoggingEnabled(enabled)`
    - `smartFixer.setNetworkLoggingEnabled(enabled)`
  - Ensure enabled state only turns on `HEADERS` logging and keeps auth header redaction (no bodies).

- First-use disclosures (locked decision):
  - Before starting dictation recording (audio mode), if `DISCLOSURE_SHOWN_DICTATION_AUDIO` is false:
    - show a modal/bottom sheet with disclosure content from `PrivacyDisclosureFormatter`
    - buttons: Continue / Cancel / Open Privacy & Safety
    - “Open Privacy & Safety” must deep-link to the Privacy & Safety route (same destination identifier/extra used by blocked sheets, e.g. `"privacy_safety"`).
    - only start recording if user taps Continue
    - set the flag true on Continue
  - Before running Smart Fix (enhancement text mode), if `DISCLOSURE_SHOWN_ENHANCEMENT_TEXT` is false:
    - show the same style of disclosure
    - only send to Smart Fix if user taps Continue; otherwise skip Smart Fix and fall back to raw transcript
    - “Open Privacy & Safety” must deep-link to the Privacy & Safety route (same destination identifier/extra used by blocked sheets, e.g. `"privacy_safety"`).
    - set the flag true on Continue

- UI constraints:
  - Disclosure should appear once per mode and never again unless reset.
  - Must not include API keys.
  - Must show base domain + endpoint paths.
  </action>
  <verify>
JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew :app:assembleDebug
  </verify>
  <done>
- First-use disclosure blocks sending until accepted (mode-specific).
- If Smart Fix disclosure is declined, no Smart Fix request is sent and raw transcript is inserted.
- Verbose logs toggle enables only redacted HEADERS logging.
  </done>
</task>

</tasks>

<verification>
- Build: `JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew assembleDebug`
</verification>

<success_criteria>
- PRIV-05 satisfied: settings disclosure + first-use modal gating exist and reflect real endpoints.
- PRIV-03 diagnostics satisfied: optional verbose logs remain redacted (no payload bodies, no auth headers).
</success_criteria>

<output>
After completion, create `.planning/phases/01-privacy-and-safety-controls/01-05-SUMMARY.md`
</output>
