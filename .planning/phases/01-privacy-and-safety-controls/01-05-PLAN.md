---
phase: 01-privacy-and-safety-controls
plan: 05
type: execute
wave: 4
depends_on:
  - "01-01"
  - "01-04"
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatter.kt
  - android/app/src/test/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatterTest.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreen.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/WhisperTranscriber.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/SmartFixer.kt
autonomous: true
requirements:
  - PRIV-03
  - PRIV-05
must_haves:
  truths:
    - Privacy & Safety clearly shows what data is sent and which endpoint base/path receives it for each mode.
    - The first external send is gated once globally until the user explicitly continues or cancels, while the disclosure copy stays mode-specific.
    - Verbose networking diagnostics are user-controllable but remain redacted HEADERS-only.
  artifacts:
    - path: android/app/src/main/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatter.kt
      provides: Live disclosure generation from provider configuration.
    - path: android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreen.kt
      provides: Disclosure and diagnostics settings UI.
    - path: android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
      provides: Global first-use disclosure gating before sends with mode-specific modal content.
  key_links:
    - from: android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
      to: android/app/src/main/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatter.kt
      via: first-use modal content generation for the pending send mode
      pattern: PrivacyDisclosureFormatter\.(disclosureForDictation|disclosureForEnhancement|disclosureForCommand)|FirstUseDisclosureUiState
    - from: android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
      to: android/app/src/main/java/com/github/shekohex/whisperpp/WhisperTranscriber.kt
      via: verbose logging preference observation
      pattern: VERBOSE_NETWORK_LOGS_ENABLED|setNetworkLoggingEnabled
---

<objective>
Expose transparent data disclosures and privacy-safe diagnostics controls.

Purpose: satisfy PRIV-05 and complete PRIV-03 by showing exactly what leaves the device while keeping verbose diagnostics redacted.
Output: endpoint disclosure formatter, Privacy & Safety disclosure UI, one-time first-use gating with mode-specific copy, and a redacted verbose logging toggle.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-privacy-and-safety-controls/01-CONTEXT.md
@.planning/phases/01-privacy-and-safety-controls/01-RESEARCH.md
@android/app/src/main/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatter.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreen.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt

<interfaces>
Live disclosure formatter contract already matches the target feature shape:

```kotlin
object PrivacyDisclosureFormatter {
    enum class Mode { DICTATION_AUDIO, ENHANCEMENT_TEXT, COMMAND_TEXT }
    data class EndpointDisclosure(val baseUrl: String, val path: String)
    data class ModeDisclosure(...)
    fun disclosureForDictation(provider: ServiceProvider?, selectedModelId: String, useContext: Boolean): ModeDisclosure
    fun disclosureForEnhancement(provider: ServiceProvider?, selectedModelId: String, useContext: Boolean): ModeDisclosure
    fun disclosureForCommand(provider: ServiceProvider?, selectedModelId: String, useContext: Boolean): ModeDisclosure
}
```

```kotlin
data class FirstUseDisclosureUiState(
    val title: String,
    val dataSent: String,
    val endpointLines: List<String>,
    val contextLine: String,
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add live endpoint disclosure generation with unit coverage</name>
  <files>android/app/src/main/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatter.kt, android/app/src/test/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatterTest.kt</files>
  <behavior>
    - Test 1: OpenAI-compatible base URLs derive audio `/audio/transcriptions` and text `/chat/completions` paths from the configured base path.
    - Test 2: Gemini disclosures normalize to `/models/{model}:generateContent`.
    - Test 3: displayed base URLs preserve scheme, host, and non-default port while stripping query strings.
  </behavior>
  <action>Write the failing formatter tests first, then implement disclosure helpers that derive endpoint base URLs and operation paths from live provider config using `HttpUrl`. Do not hardcode full URLs or include auth data. Include mode-specific copy for dictation audio, enhancement text, and command text, plus an explicit context line describing whether before-cursor context can be sent.</action>
  <verify>
    <automated>./android/gradlew testDebugUnitTest --tests "com.github.shekohex.whisperpp.privacy.PrivacyDisclosureFormatterTest"</automated>
  </verify>
  <done>Disclosures are generated from real provider configuration, not duplicated strings, and formatter tests lock down endpoint derivation.</done>
</task>

<task type="auto">
  <name>task 2: extend Privacy and Safety settings with disclosures and diagnostics controls</name>
  <files>android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt, android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreen.kt</files>
  <action>Add the persistence keys for `VERBOSE_NETWORK_LOGS_ENABLED` and one global first-use acknowledgment flag such as `FIRST_USE_DISCLOSURE_ACKNOWLEDGED`. In `PrivacySafetyScreen`, render a disclosure section backed by `PrivacyDisclosureFormatter`, a diagnostics section whose copy explicitly states that verbose logging still excludes auth headers and payload bodies, and a reset action that clears that single acknowledgment flag. Keep this screen the primary disclosure surface per the locked decision.</action>
  <verify>
    <automated>./android/gradlew assembleDebug</automated>
  </verify>
  <done>Settings show live disclosures for each mode, the verbose logging toggle persists, and users can reset the one global first-use disclosure acknowledgment from Privacy & Safety.</done>
</task>

<task type="auto">
  <name>task 3: gate the first external send globally and wire the verbose diagnostics toggle to both network clients</name>
  <files>android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt, android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt, android/app/src/main/java/com/github/shekohex/whisperpp/WhisperTranscriber.kt, android/app/src/main/java/com/github/shekohex/whisperpp/SmartFixer.kt</files>
  <action>Observe `VERBOSE_NETWORK_LOGS_ENABLED` in the IME and call `setNetworkLoggingEnabled(enabled)` on both network clients, keeping them HEADERS-only. Before dictation audio starts, enhancement sends, and command-mode sends, check one global first-use acknowledgment flag. If the flag is unset, build the pending modal content in `WhisperInputService`/`KeyboardScreen` from `PrivacyDisclosureFormatter.disclosureForDictation`, `disclosureForEnhancement`, or `disclosureForCommand` and map it into `FirstUseDisclosureUiState`. Show a first-use disclosure modal with Continue, Cancel, and Open Privacy & Safety actions. Continue sets the single global acknowledgment flag and proceeds; Cancel aborts the send; declining enhancement must fall back to raw transcript insertion rather than sending Smart Fix text. After the user acknowledges once, later sends in any mode proceed without another gate until Privacy & Safety resets that one flag.</action>
  <verify>
    <automated>./android/gradlew assembleDebug</automated>
  </verify>
  <done>Users must explicitly accept the first external send once globally before data leaves the device, the modal copy always comes from `PrivacyDisclosureFormatter`, and enabling verbose diagnostics never escalates beyond redacted headers.</done>
</task>

</tasks>

<verification>
- `./android/gradlew testDebugUnitTest --tests "com.github.shekohex.whisperpp.privacy.PrivacyDisclosureFormatterTest"`
- `./android/gradlew assembleDebug`
</verification>

<success_criteria>
- PRIV-05 is satisfied through settings disclosures and one global first-use gate with mode-specific copy.
- PRIV-03 remains satisfied when verbose diagnostics are enabled.
</success_criteria>

<output>
After completion, create `.planning/phases/01-privacy-and-safety-controls/01-05-SUMMARY.md`
</output>
