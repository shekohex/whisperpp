---
phase: 01-privacy-and-safety-controls
plan: 06
type: execute
wave: 6
depends_on:
  - "01-05"
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/BlockedExplanationCopySpec.kt
  - android/app/src/main/res/values/strings.xml
  - android/app/src/test/java/com/github/shekohex/whisperpp/ui/keyboard/BlockedExplanationCopySpecTest.kt
  - android/app/src/androidTest/java/com/github/shekohex/whisperpp/ui/keyboard/BlockedExplanationContentUiTest.kt
autonomous: false
gap_closure: true

must_haves:
  truths:
    - "In a password/OTP-like field, the main action is locked/disabled and external sending does not start; tapping the locked action shows a non-empty explanation sheet with the block reason"
    - "When blocked by per-app Privacy & Safety policy, the main action is locked/disabled and external sending does not start; tapping the locked action shows a non-empty explanation sheet stating app-policy block"
  artifacts:
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt"
      provides: "IME-safe blocked explanation presentation (expanded sheet + fallback)"
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/BlockedExplanationCopySpec.kt"
      provides: "Deterministic blocked-explanation copy spec (res ids + args)"
    - path: "android/app/src/main/res/values/strings.xml"
      provides: "App-policy blocked explanation strings (title/description/reason)"
    - path: "android/app/src/test/java/com/github/shekohex/whisperpp/ui/keyboard/BlockedExplanationCopySpecTest.kt"
      provides: "Regression test for blocked explanation copy selection"
    - path: "android/app/src/androidTest/java/com/github/shekohex/whisperpp/ui/keyboard/BlockedExplanationContentUiTest.kt"
      provides: "Regression UI test: blocked explanation content is visible/non-empty in constrained host"
  key_links:
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt"
      to: "androidx.compose.material3.ModalBottomSheet"
      via: "explicit sheetState + show() to force expanded"
      pattern: "rememberModalBottomSheetState\(skipPartiallyExpanded\s*=\s*true\)"
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt"
      to: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/BlockedExplanationCopySpec.kt"
      via: "blocked explanation copy resolution"
      pattern: "blockedExplanationCopySpec\("
---

<objective>
Fix the secure-field and app-policy blocked explanation UI so it reliably shows non-empty content in the IME host.

Purpose: Close diagnosed Phase 1 UAT gaps where the blocked explanation bottom sheet appeared empty.
Output: Expanded/IME-safe blocked explanation presentation + copy spec + regression test.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-privacy-and-safety-controls/01-UAT.md

@android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
@android/app/src/main/res/values/strings.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make blocked explanation sheet IME-safe and always expanded</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/BlockedExplanationCopySpec.kt
android/app/src/main/res/values/strings.xml
  </files>
  <action>
- Add missing app-policy explanation strings to `strings.xml` and stop hardcoding app-policy title/description/reason in `KeyboardScreen.kt`.
  - Add (names are REQUIRED):
    - `blocked_app_policy_sheet_title`
    - `blocked_app_policy_sheet_description`
    - `blocked_app_policy_reason_generic`
    - `blocked_app_policy_reason_for_package` (format arg: packageName)

- Create `BlockedExplanationCopySpec.kt` (package: `com.github.shekohex.whisperpp.ui.keyboard`) with:
  - `internal data class BlockedExplanationCopySpec(...)` containing:
    - `@StringRes titleRes`, `@StringRes descriptionRes`, `@StringRes reasonRes`
    - `reasonArg: String? = null`
    - `showDontShowAgain: Boolean`
  - `internal fun blockedExplanationCopySpec(externalSendBlockedReason: SecureFieldDetector.Reason?, externalSendBlockedByAppPolicy: Boolean, blockedPackageName: String?): BlockedExplanationCopySpec`
    - If `externalSendBlockedByAppPolicy == true`:
      - title/description use the new app-policy strings.
      - reason uses `blocked_app_policy_reason_for_package` when `blockedPackageName` is non-blank; otherwise `blocked_app_policy_reason_generic`.
      - `showDontShowAgain = false`
 - Else (secure-field flow):
       - title/description MUST be:
         - `R.string.secure_field_sheet_title`
         - `R.string.secure_field_sheet_description`
       - reason MUST map 1:1:
         - `PasswordLike` -> `R.string.secure_field_reason_password`
         - `OtpLike` -> `R.string.secure_field_reason_otp`
         - `NoPersonalizedLearning` -> `R.string.secure_field_reason_no_personalized_learning`
         - `Unknown` (or null) -> `R.string.secure_field_reason_unknown`
       - `showDontShowAgain = true`

- Update the blocked explanation UI in `KeyboardScreen.kt` to:
   - Compute a single `val copy = blockedExplanationCopySpec(...)` and render all text via `stringResource(copy.*Res)` (use the arg overload when `copy.reasonArg != null`).
  - The sheet content MUST include these secure-field resources when secure-field blocked:
    - Title: `R.string.secure_field_sheet_title`
    - Description: `R.string.secure_field_sheet_description`
    - Open settings CTA: `R.string.secure_field_sheet_open_settings`
    - Don’t show again: `R.string.secure_field_sheet_dont_show_again` (only when `copy.showDontShowAgain == true`)
   - Use an explicit `ModalBottomSheet` state for the blocked explanation:
     - `rememberModalBottomSheetState(skipPartiallyExpanded = true)`.
     - Drive opening via a shared blocked-explanation visibility state (NOT secure-only):
       - Use a single `showBlockedExplanation` (or equivalent) that becomes true when the user taps the locked main action for either secure-field OR app-policy blocks.
       - Use `LaunchedEffect(showBlockedExplanation)` calling `sheetState.show()` when it becomes true.
     - Pass `windowInsets = WindowInsets(0, 0, 0, 0)` to the blocked explanation `ModalBottomSheet` to prevent IME/safe-area insets pushing content out of view.
     - Make the sheet content scrollable (`verticalScroll`) so content stays reachable in constrained height.

- Add an IME-safe fallback if the sheet still can't expand:
   - If `showBlockedExplanation` becomes true and `sheetState.currentValue` does not reach Expanded within ~300ms, close the sheet and show an in-keyboard `Popup` overlay containing the same explanation content.
   - The fallback overlay must include a close action and the same "Open Privacy & Safety settings" action.
   - Reuse the same extracted content composable for both sheet and fallback, so secure-field + app-policy flows render identical content.
  </action>
  <verify>
JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew assembleDebug
  </verify>
  <done>
- Blocked explanation uses explicit expanded sheet state and renders via shared content composable.
- Sheet opening is driven by a shared blocked-explanation visibility state for both secure-field and app-policy blocks.
- App-policy explanation strings live in `strings.xml` (no hardcoded strings for that sheet in `KeyboardScreen.kt`).
- Fallback overlay renders the same non-empty content when sheet expansion fails.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add regression tests (copy selection + constrained-host visible content)</name>
  <files>
android/app/src/test/java/com/github/shekohex/whisperpp/ui/keyboard/BlockedExplanationCopySpecTest.kt
android/app/src/androidTest/java/com/github/shekohex/whisperpp/ui/keyboard/BlockedExplanationContentUiTest.kt
  </files>
  <action>
- Add JUnit4 unit tests for `blockedExplanationCopySpec(...)` covering:
  - Secure-field flow (externalSendBlockedByAppPolicy=false):
    - For each `SecureFieldDetector.Reason` value, returned `titleRes/descriptionRes/reasonRes` are the expected secure-field resources.
      - Expect title/description always:
        - `R.string.secure_field_sheet_title`
        - `R.string.secure_field_sheet_description`
      - Expect reason mapping:
        - `PasswordLike` -> `R.string.secure_field_reason_password`
        - `OtpLike` -> `R.string.secure_field_reason_otp`
        - `NoPersonalizedLearning` -> `R.string.secure_field_reason_no_personalized_learning`
        - `Unknown` -> `R.string.secure_field_reason_unknown`
    - `showDontShowAgain == true`.
  - App-policy flow (externalSendBlockedByAppPolicy=true):
    - Uses app-policy title/description resources.
    - When packageName is blank/null -> reasonRes is `blocked_app_policy_reason_generic` and `reasonArg == null`.
    - When packageName is non-blank -> reasonRes is `blocked_app_policy_reason_for_package` and `reasonArg == <packageName>`.
    - `showDontShowAgain == false`.

- Add an Android instrumentation Compose UI test that renders the exact blocked-explanation content composable used by both the sheet and fallback inside a constrained host (IME-like):
  - In test `setContent { ... }`, wrap the explanation content in a container like `Box(Modifier.requiredHeight(220.dp).requiredWidth(360.dp))`.
  - Ensure the content is inside a vertical scroll container (as implemented in Task 1) so `performScrollTo()` works in the test.

- Test cases (must assert nodes are displayed and text is non-empty / not blank):
  - Secure-field (password-like):
    - Use `blockedExplanationCopySpec(externalSendBlockedReason = SecureFieldDetector.Reason.PasswordLike, externalSendBlockedByAppPolicy = false, blockedPackageName = null)`.
    - Assert visible text includes:
      - `R.string.secure_field_sheet_title`
      - `R.string.secure_field_sheet_description`
      - `R.string.secure_field_reason_password`
      - `R.string.secure_field_sheet_open_settings`
      - `R.string.secure_field_sheet_dont_show_again`
  - App-policy (with package):
    - Use `blockedExplanationCopySpec(externalSendBlockedReason = null, externalSendBlockedByAppPolicy = true, blockedPackageName = "com.example.app")`.
    - Assert visible text includes:
      - `R.string.blocked_app_policy_sheet_title`
      - `R.string.blocked_app_policy_sheet_description`
      - A reason line that contains "com.example.app" (from `R.string.blocked_app_policy_reason_for_package`).
    - Assert `R.string.secure_field_sheet_dont_show_again` does NOT exist.

- Use Compose test APIs to prove “visible in constrained host” (not just exists):
  - For each asserted string node, call `performScrollTo()` (if needed) then `assertIsDisplayed()`.
  </action>
  <verify>
JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew testDebugUnitTest :app:assembleDebugAndroidTest

if command -v adb >/dev/null 2>&1 && adb get-state >/dev/null 2>&1; then
  JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.github.shekohex.whisperpp.ui.keyboard.BlockedExplanationContentUiTest
fi
  </verify>
  <done>
- Unit tests pass and prevent regressions where explanation copy could resolve to missing/incorrect resources.
- `:app:assembleDebugAndroidTest` succeeds (instrumentation test compiles). If a device/emulator is attached, `connectedDebugAndroidTest` passes and executes `BlockedExplanationContentUiTest`, enforcing that blocked explanation content is visible/non-empty in a constrained host for both secure-field and app-policy flows.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Blocked explanation UI now shows non-empty content for both secure-field and app-policy blocks (sheet + IME-safe fallback).</what-built>
  <how-to-verify>
1) Install debug build on device (or run from Android Studio) and enable Whisper++ keyboard.
2) Secure-field flow:
   - Open any app with a password field (or OTP field).
   - Focus the field so Whisper++ is visible.
   - Confirm the main action is locked/disabled.
   - Tap the locked action.
   - Expected: an explanation UI appears with a title, description, and a "Reason: ..." line (NOT blank).
3) App-policy flow:
   - Open Whisper++ Settings -> Privacy & Safety and block the current target app (or any app you can test in).
   - Return to that app and focus a normal (non-secure) text field.
   - Confirm the main action is locked/disabled.
   - Tap the locked action.
   - Expected: explanation UI appears and explicitly says external sending is blocked by app policy (NOT blank).
4) Optional stress: rotate screen / switch apps and retest; explanation content should still render.
  </how-to-verify>
  <resume-signal>Type "approved" or describe exactly what text/area is still empty.</resume-signal>
</task>

</tasks>

<verification>
- `assembleDebug`, `testDebugUnitTest`, and `:app:assembleDebugAndroidTest` succeed.
- If a device/emulator is attached, `connectedDebugAndroidTest` runs `BlockedExplanationContentUiTest` successfully.
- Human verification confirms explanation content is visible/non-empty in IME for both secure-field and app-policy blocks.
</verification>

<success_criteria>
- The Phase 1 UAT gaps (tests 1 and 5) are closed: tapping the locked action shows non-empty blocked explanation content for both secure-field and app-policy flows.
</success_criteria>

<output>
After completion, create `.planning/phases/01-privacy-and-safety-controls/01-06-SUMMARY.md`
</output>
