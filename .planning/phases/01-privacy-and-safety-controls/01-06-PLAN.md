---
phase: 01-privacy-and-safety-controls
plan: 06
type: execute
wave: 5
depends_on:
  - "01-05"
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/BlockedExplanationCopySpec.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/BlockedExplanationActivity.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
  - android/app/src/main/res/values/strings.xml
  - android/app/src/test/java/com/github/shekohex/whisperpp/ui/keyboard/BlockedExplanationCopySpecTest.kt
  - android/app/src/androidTest/java/com/github/shekohex/whisperpp/ui/keyboard/BlockedExplanationContentUiTest.kt
autonomous: false
requirements:
  - PRIV-01
  - PRIV-04
must_haves:
  truths:
    - Secure-field blocks always explain themselves with non-empty content in the IME host.
    - App-policy blocks reuse the same explanation surface with app-policy-specific copy and no don't-show-again control.
    - If the IME bottom sheet cannot render reliably, the fallback explanation surface shows the same content.
  artifacts:
    - path: android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/BlockedExplanationCopySpec.kt
      provides: Deterministic resource-based copy selection for secure-field and app-policy blocks.
    - path: android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
      provides: IME-safe blocked explanation sheet and fallback host.
    - path: android/app/src/androidTest/java/com/github/shekohex/whisperpp/ui/keyboard/BlockedExplanationContentUiTest.kt
      provides: Regression coverage for visible blocked explanation content in a constrained host.
  key_links:
    - from: android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
      to: android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/BlockedExplanationCopySpec.kt
      via: shared explanation content
      pattern: blockedExplanationCopySpec\(
    - from: android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
      to: android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/BlockedExplanationActivity.kt
      via: host fallback when sheet rendering fails
      pattern: BlockedExplanationActivity|rememberModalBottomSheetState
---

<objective>
Harden blocked explanations so privacy blocks are always understandable in the constrained IME host.

Purpose: finish PRIV-01 and PRIV-04 UX quality by making secure-field and app-policy explanations reliable, visible, and regression-tested.
Output: deterministic copy selection, IME-safe explanation presentation with fallback host, regression tests, and a final human verification checkpoint.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-privacy-and-safety-controls/01-CONTEXT.md
@.planning/phases/01-privacy-and-safety-controls/01-RESEARCH.md
@android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
@android/app/src/main/res/values/strings.xml

<interfaces>
Existing blocked explanation contracts:

```kotlin
internal data class BlockedExplanationCopySpec(
    @StringRes val titleRes: Int,
    @StringRes val descriptionRes: Int,
    @StringRes val reasonRes: Int,
    val reasonArg: String? = null,
    val showDontShowAgain: Boolean,
)
```

```kotlin
internal fun blockedExplanationCopySpec(
    externalSendBlockedReason: SecureFieldDetector.Reason?,
    externalSendBlockedByAppPolicy: Boolean,
    blockedPackageName: String?,
): BlockedExplanationCopySpec
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: lock down blocked explanation copy selection and strings</name>
  <files>android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/BlockedExplanationCopySpec.kt, android/app/src/main/res/values/strings.xml, android/app/src/test/java/com/github/shekohex/whisperpp/ui/keyboard/BlockedExplanationCopySpecTest.kt</files>
  <behavior>
    - Test 1: each secure-field reason maps to the secure-field title, description, and exact reason string resource.
    - Test 2: app-policy without a package uses the generic app-policy reason and disables don't-show-again.
    - Test 3: app-policy with a package name uses the package-aware reason resource and still hides don't-show-again.
  </behavior>
  <action>Write the copy-spec tests first, then extract any remaining hardcoded explanation text into `strings.xml` and keep `blockedExplanationCopySpec()` as the single source of truth for secure-field vs app-policy copy selection. Preserve the locked decision that `Don't show again` applies only to secure-field explanations, never to app-policy blocks.</action>
  <verify>
    <automated>./android/gradlew testDebugUnitTest --tests "com.github.shekohex.whisperpp.ui.keyboard.BlockedExplanationCopySpecTest"</automated>
  </verify>
  <done>Explanation copy is deterministic, resource-backed, and unit tested for both secure-field and app-policy flows.</done>
</task>

<task type="auto">
  <name>task 2: make blocked explanation rendering IME-safe and add constrained-host regression coverage</name>
  <files>android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt, android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/BlockedExplanationActivity.kt, android/app/src/androidTest/java/com/github/shekohex/whisperpp/ui/keyboard/BlockedExplanationContentUiTest.kt</files>
  <action>Use one shared blocked-explanation content composable for secure-field and app-policy blocks. Present it from `KeyboardScreen` with an explicit `rememberModalBottomSheetState(skipPartiallyExpanded = true)`, scrollable content, and zero insets so the sheet can render inside an IME host. If the sheet host still fails to present the content reliably, route to `BlockedExplanationActivity` and render the exact same content there. Add a constrained-host Compose UI regression test that proves the explanation title, description, reason line, and settings CTA are visible for both secure-field and app-policy flows.</action>
  <verify>
    <automated>./android/gradlew connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.github.shekohex.whisperpp.ui.keyboard.BlockedExplanationContentUiTest</automated>
  </verify>
  <done>Blocked explanations are visible in constrained hosts, the fallback host reuses the same content, and the instrumentation test runs with assertions for both block types.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>task 3: verify blocked explanations in a real IME host</name>
  <action>Run the automated build/test checks first, then pause for a device-level IME verification because bottom-sheet rendering can differ across Android versions and keyboard hosts.</action>
  <what-built>Reliable blocked explanation UI for both secure-field and app-policy privacy blocks.</what-built>
  <how-to-verify>
1. Install the debug build and enable the Whisper++ keyboard.
2. Open any password or OTP field, confirm the main action is locked, then tap it.
3. Expected: non-empty explanation content appears with title, description, reason, and Privacy & Safety action.
4. In Settings → Privacy & Safety, block a test app, return to that app, focus a normal text field, and tap the locked action.
5. Expected: non-empty app-policy explanation appears, and it does not show the secure-field don't-show-again control.
  </how-to-verify>
  <verify>
    <automated>./android/gradlew connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.github.shekohex.whisperpp.ui.keyboard.BlockedExplanationContentUiTest</automated>
  </verify>
  <done>Human validation confirms secure-field and app-policy blocked explanations both render non-empty content in the real IME host.</done>
  <resume-signal>Type "approved" or describe the exact missing/blank explanation surface.</resume-signal>
</task>

</tasks>

<verification>
- `./android/gradlew testDebugUnitTest --tests "com.github.shekohex.whisperpp.ui.keyboard.BlockedExplanationCopySpecTest"`
- `./android/gradlew connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.github.shekohex.whisperpp.ui.keyboard.BlockedExplanationContentUiTest`
- Human verification confirms non-empty explanation content for secure-field and app-policy blocks.
</verification>

<success_criteria>
- Secure-field and app-policy blocks always explain themselves in the IME host.
- Regression coverage exists for both copy selection and constrained-host content rendering.
</success_criteria>

<output>
After completion, create `.planning/phases/01-privacy-and-safety-controls/01-06-SUMMARY.md`
</output>
