---
phase: 01-privacy-and-safety-controls
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/privacy/SecureFieldDetector.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
autonomous: true

must_haves:
  truths:
    - "In secure fields, dictation and command-mode actions are unavailable and user can see why"
    - "If focus moves into a secure field while active, sending stops immediately"
  artifacts:
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/privacy/SecureFieldDetector.kt"
      provides: "Strict secure-field detection (password/OTP/privacy-flagged)"
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt"
      provides: "Secure-field send gate + immediate stop on secure focus"
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt"
      provides: "Disabled mic/send UI with lock icon + details sheet"
  key_links:
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt"
      to: "android.view.inputmethod.EditorInfo"
      via: "onStartInput/onStartInputView"
      pattern: "onStartInput(View)?\\(.*EditorInfo"
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt"
      to: "WhisperTranscriber.stop"
      via: "secure-field immediate stop"
      pattern: "stop\\(\\)"
---

<objective>
Implement strict secure-field detection and a central IME send gate so dictation cannot start in secure fields; disabled UI explains why; entering a secure field while active stops immediately.

Purpose: Meet PRIV-01 (secure fields) and enforce privacy stop behavior.
Output: Secure-field detector + IME gate + keyboard UX (disabled + details sheet + don't show again).
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/01-privacy-and-safety-controls/01-CONTEXT.md
@.planning/phases/01-privacy-and-safety-controls/01-RESEARCH.md

@android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add strict secure-field detection helper</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/privacy/SecureFieldDetector.kt
  </files>
  <action>
- Create `SecureFieldDetector` as a pure utility:
  - Input: `EditorInfo?`
  - Output: structured result that includes:
    - `isSecure: Boolean`
    - `reason: PasswordLike | OtpLike | NoPersonalizedLearning | Unknown`

- Detection must follow locked decisions (strict):
  - Password-like: `InputType` password variations for text + number password.
  - Privacy-flagged: `EditorInfo.imeOptions` includes `IME_FLAG_NO_PERSONALIZED_LEARNING`.
  - OTP-like: conservative heuristic based on hint text + numeric/password input types.

- OTP heuristic:
  - Use lowercased `editorInfo.hintText` (when present) and match strong tokens only (otp, one-time, verification, 2fa, passcode, pin, code).
  - Avoid aggressive matching that would block normal numeric fields.
  </action>
  <verify>
JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew :app:assembleDebug
  </verify>
  <done>
- A reusable detector exists and compiles.
- Detector covers password-like + IME no-personalized-learning + conservative OTP.
  </done>
</task>

<task type="auto">
  <name>Task 2: Enforce secure-field gate in IME + disabled UI with details sheet</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt
android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
  </files>
  <action>
- DataStore key:
  - Add a boolean preference key `SECURE_FIELD_EXPLANATION_DONT_SHOW_AGAIN` (global).

- IME “external-send gate” contract (applies to dictation + Smart Fix + future command mode):
  - In `WhisperInputService`, create a single function that returns a “send block” (or null) for the current editor.
  - Contract: every entry point that can trigger external sending must consult this gate before doing work (dictation start, Smart Fix request, and any future command-mode send/action dispatch).
  - Update editor tracking by overriding `onStartInput(editorInfo, restarting)` and `onStartInputView(editorInfo, restarting)`.
  - If state is active (recording/transcribing/smart-fixing) and focus enters a secure field:
    - stop recorder immediately
    - call `whisperTranscriber.stop()`
    - call `smartFixer.cancel()` (added in 01-01)
    - reset keyboard state to Ready
    - show a short, safe notice (Toast) like “Secure field: sending stopped”.

- Prevent starts in secure fields:
  - When secure-field blocked, all external-send entry points must be blocked by the same gate (dictation start, Smart Fix, and any future command-mode entry points).
  - Mic/send actions must not start recording/transcription.
  - Mic UI remains visible but disabled + lock icon.

- Keyboard UX (locked decisions):
  - Modify `KeyboardScreen`/`MainActionButton` to accept a new “external sending blocked” state and a callback for tap-on-blocked.
  - When blocked:
    - show lock icon (or mic-with-lock)
    - disable swipe/long-press behavior and any action start
    - tapping shows a ModalBottomSheet with:
      - reason (password/OTP/no-personalized-learning)
      - a button to open Settings (deep-link to Privacy & Safety once available):
        - Implement the callback to request a specific settings destination (string/extra), e.g. `"privacy_safety"`.
        - For now (before 01-04 adds the route), fall back to opening Settings main if the destination is not recognized.
        - Do NOT hardcode an activity/class name from the keyboard UI; route via the existing settings open mechanism in `MainActivity`.
      - a “Don’t show again” control that writes `SECURE_FIELD_EXPLANATION_DONT_SHOW_AGAIN=true`.
  - No proactive banner; only show the sheet when user taps the disabled action.
  </action>
  <verify>
JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew :app:assembleDebug
  </verify>
  <done>
- Secure fields: mic action cannot start; disabled UI shows lock.
- Tapping disabled mic shows details sheet (unless “don’t show again” was set).
- Entering a secure field while active stops immediately and cancels in-flight network.
- Secure fields: command mode (or any future command entry points that can send externally) is blocked by the same IME gate and must reuse the same explanation path.
  </done>
</task>

</tasks>

<verification>
- Build: `JAVA_HOME="/Users/shady/Library/Java/JavaVirtualMachines/jbr-17.0.9/Contents/Home" ./android/gradlew assembleDebug`
</verification>

<success_criteria>
- PRIV-01 secure-field behavior works end-to-end (block + explain + immediate stop).
</success_criteria>

<output>
After completion, create `.planning/phases/01-privacy-and-safety-controls/01-02-SUMMARY.md`
</output>
