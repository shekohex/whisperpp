---
phase: 01-privacy-and-safety-controls
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/privacy/SecureFieldDetector.kt
  - android/app/src/test/java/com/github/shekohex/whisperpp/privacy/SecureFieldDetectorTest.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
  - android/app/src/main/res/values/strings.xml
autonomous: true
requirements:
  - PRIV-01
must_haves:
  truths:
    - Password-like, OTP-like, and privacy-flagged fields block external sending.
    - Mic and command affordances stay visible but locked, with explanation only on tap.
    - Moving focus into a secure field while dictation or enhancement is active stops sending immediately.
  artifacts:
    - path: android/app/src/main/java/com/github/shekohex/whisperpp/privacy/SecureFieldDetector.kt
      provides: Structured secure-field detection with explicit reasons.
    - path: android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
      provides: Central external-send gate for secure-field enforcement.
    - path: android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
      provides: Locked-action UX, tap-to-explain flow, and don't-show-again handling.
  key_links:
    - from: android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
      to: android/app/src/main/java/com/github/shekohex/whisperpp/privacy/SecureFieldDetector.kt
      via: current external-send decision
      pattern: SecureFieldDetector\.detect|currentExternalSendBlock
    - from: android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
      to: android/app/src/main/java/com/github/shekohex/whisperpp/SmartFixer.kt
      via: secure-field stop path
      pattern: whisperTranscriber\.stop|smartFixer\.cancel
---

<objective>
Implement secure-field protection end to end.

Purpose: satisfy PRIV-01 with a single IME gate that blocks accidental capture in secure editors and explains the block without noisy banners.
Output: secure-field detector, secure stop behavior, locked keyboard actions, and secure-field explanation preference wiring.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-privacy-and-safety-controls/01-CONTEXT.md
@.planning/phases/01-privacy-and-safety-controls/01-RESEARCH.md
@android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt

<interfaces>
Locked routing and UI contracts already present in the codebase:

```kotlin
const val EXTRA_SETTINGS_DESTINATION = "settings_destination"
val SECURE_FIELD_EXPLANATION_DONT_SHOW_AGAIN =
    booleanPreferencesKey("secure-field-explanation-dont-show-again")
```

```kotlin
object SecureFieldDetector {
    enum class Reason { PasswordLike, OtpLike, NoPersonalizedLearning, Unknown }
    data class Result(val isSecure: Boolean, val reason: Reason)
    fun detect(editorInfo: EditorInfo?): Result
}
```

```kotlin
fun KeyboardScreen(
    ...,
    onOpenSettingsDestination: (String) -> Unit = {},
    externalSendBlockedReason: SecureFieldDetector.Reason? = null,
    showSecureFieldExplanation: Boolean = true,
    onBlockedAction: () -> Unit = {},
    onDontShowSecureFieldExplanationAgain: () -> Unit = {},
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: add strict secure-field detection with unit coverage</name>
  <files>android/app/src/main/java/com/github/shekohex/whisperpp/privacy/SecureFieldDetector.kt, android/app/src/test/java/com/github/shekohex/whisperpp/privacy/SecureFieldDetectorTest.kt</files>
  <behavior>
    - Test 1: password variations return `isSecure=true` with `PasswordLike`.
    - Test 2: `IME_FLAG_NO_PERSONALIZED_LEARNING` returns `NoPersonalizedLearning` even without password flags.
    - Test 3: conservative OTP hints only block numeric/password-like fields with strong OTP tokens and do not block ordinary numeric inputs.
  </behavior>
  <action>Write failing unit tests first, then implement `SecureFieldDetector.detect(EditorInfo?)` as a pure function honoring the locked decisions exactly: password-like fields, OTP-like fields, and privacy-flagged fields must all resolve to secure results with explicit reasons. Keep OTP matching conservative to avoid false positives in normal numeric inputs.</action>
  <verify>
    <automated>./android/gradlew testDebugUnitTest --tests "com.github.shekohex.whisperpp.privacy.SecureFieldDetectorTest"</automated>
  </verify>
  <done>The detector is reusable from IME and UI layers and the tests lock down the password/OTP/privacy-flagged behavior.</done>
</task>

<task type="auto">
  <name>task 2: wire the secure-field send gate and locked-action explanation flow</name>
  <files>android/app/src/main/java/com/github/shekohex/whisperpp/MainActivity.kt, android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt, android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt, android/app/src/main/res/values/strings.xml</files>
  <action>In `WhisperInputService`, create one secure-field gate consulted before every external-send entry point: dictation start, enhancement send, and command-mode entry/send hooks. Override `onStartInput` and `onStartInputView` so focus changes recompute the block. If focus moves into a secure field while recording/transcribing/enhancing, stop recorder work immediately, call both `whisperTranscriber.stop()` and `smartFixer.cancel()`, reset UI state, and show only a short secure-field notice. In `KeyboardScreen`, keep the action visible but locked, remove proactive banners, and open the explanation flow only when the user taps the locked action. The explanation must include the secure-field reason, an `Open Privacy & Safety` action routed through `EXTRA_SETTINGS_DESTINATION`, and a global `Don't show again` toggle backed by `SECURE_FIELD_EXPLANATION_DONT_SHOW_AGAIN`.</action>
  <verify>
    <automated>./android/gradlew assembleDebug</automated>
  </verify>
  <done>Secure fields block external sending before any audio/text leaves the device, the locked action explains why on tap only, and entering a secure field mid-run stops sending immediately.</done>
</task>

</tasks>

<verification>
- `./android/gradlew testDebugUnitTest --tests "com.github.shekohex.whisperpp.privacy.SecureFieldDetectorTest"`
- `./android/gradlew assembleDebug`
</verification>

<success_criteria>
- PRIV-01 is implemented with one IME decision path, not scattered call-site checks.
- Secure-field explanations are tap-to-explain only and respect the global don't-show-again preference.
</success_criteria>

<output>
After completion, create `.planning/phases/01-privacy-and-safety-controls/01-02-SUMMARY.md`
</output>
