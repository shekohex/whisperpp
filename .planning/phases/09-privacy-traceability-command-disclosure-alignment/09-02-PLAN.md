---
phase: 09-privacy-traceability-command-disclosure-alignment
plan: 02
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatter.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreen.kt
  - android/app/src/test/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatterTest.kt
  - android/app/src/test/java/com/github/shekohex/whisperpp/command/CommandDisclosureFlowTest.kt
  - android/app/src/androidTest/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreenUiTest.kt
autonomous: true
requirements:
  - PRIV-01
  - PRIV-04
  - PRIV-05
  - CMD-03
must_haves:
  truths:
    - "Before command-mode instruction audio is recorded, the user sees an accurate command disclosure describing both the STT audio hop and the text-transform hop."
    - "Canceling or opening Privacy & Safety from the command disclosure prevents recording and leaves command mode without sending audio/text."
    - "Privacy & Safety settings and the IME first-use modal show the same command disclosure content from one shared formatter."
  artifacts:
    - path: android/app/src/main/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatter.kt
      provides: Shared command disclosure model with truthful two-hop endpoint/data copy.
    - path: android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
      provides: Pre-capture command disclosure gate and unchanged shared external-send blocking checks.
    - path: android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreen.kt
      provides: Settings disclosure surface sourced from the same live command disclosure formatter.
    - path: android/app/src/test/java/com/github/shekohex/whisperpp/command/CommandDisclosureFlowTest.kt
      provides: JVM proof that command disclosure timing stays before recording.
    - path: android/app/src/androidTest/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreenUiTest.kt
      provides: Instrumentation proof for command disclosure/reset/send-policy UI state.
  key_links:
    - from: android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
      to: android/app/src/main/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatter.kt
      via: command-mode first-use disclosure built from effective STT + text providers
      pattern: disclosureForCommand|awaitFirstUseDisclosure
    - from: android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
      to: android/app/src/main/java/com/github/shekohex/whisperpp/recorder/RecorderManager.kt
      via: disclosure gate before recorder start
      pattern: awaitFirstUseDisclosure|recorderManager\?\.start
    - from: android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreen.kt
      to: android/app/src/main/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatter.kt
      via: settings disclosure cards fed by live formatter output
      pattern: disclosureForCommand|DisclosureCard
---

<objective>
Align command-mode privacy disclosure content and timing with the shipped command pipeline.

Purpose: close the real integration bug where command mode recorded instruction audio before consent and displayed stale placeholder disclosure copy.
Output: updated formatter, IME gate, and privacy-screen coverage for truthful pre-capture command disclosures.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/09-privacy-traceability-command-disclosure-alignment/09-RESEARCH.md
@.planning/phases/09-privacy-traceability-command-disclosure-alignment/09-VALIDATION.md
@.planning/phases/01-privacy-and-safety-controls/01-05-SUMMARY.md
@.planning/phases/05-command-mode-presets/05-03-SUMMARY.md
@android/app/src/main/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatter.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreen.kt
@android/app/src/test/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatterTest.kt
@android/app/src/test/java/com/github/shekohex/whisperpp/command/CommandDisclosureFlowTest.kt
@android/app/src/androidTest/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreenUiTest.kt

<interfaces>
From android/app/src/main/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatter.kt:
```kotlin
object PrivacyDisclosureFormatter {
    enum class Mode {
        DICTATION_AUDIO,
        ENHANCEMENT_TEXT,
        COMMAND_TEXT,
    }

    data class EndpointDisclosure(
        val baseUrl: String,
        val path: String,
    )

    data class ModeDisclosure(
        val mode: Mode,
        val title: String,
        val dataSent: String,
        val endpoints: List<EndpointDisclosure>,
        val contextLine: String,
    )

    fun disclosureForCommand(
        provider: ServiceProvider?,
        selectedModelId: String,
        useContext: Boolean,
    ): ModeDisclosure
}
```

From android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt:
```kotlin
data class FirstUseDisclosureUiState(
    val title: String,
    val dataSent: String,
    val endpointLines: List<String>,
    val contextLine: String,
)
```

From android/app/src/main/java/com/github/shekohex/whisperpp/command/CommandModels.kt:
```kotlin
enum class CommandStage {
    WAITING,
    CLIPBOARD_CONFIRM,
    LISTENING,
    PROCESSING,
    DONE,
    ERROR,
}
```

From android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt:
```kotlin
private suspend fun awaitFirstUseDisclosure(
    mode: FirstUseDisclosureMode,
    disclosure: PrivacyDisclosureFormatter.ModeDisclosure,
): FirstUseDisclosureDecision

private fun onCommandEnter()
private fun startCommandListening()
private fun onCommandStopListening()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: make command disclosures truthful and shared across IME + settings</name>
  <files>android/app/src/main/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatter.kt, android/app/src/main/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreen.kt, android/app/src/test/java/com/github/shekohex/whisperpp/privacy/PrivacyDisclosureFormatterTest.kt</files>
  <behavior>
    - Test 1: command disclosure derived from live config lists two labeled hops: instruction-audio transcription endpoint from the active STT provider/model, then text-transform endpoint from the effective command text provider/model.
    - Test 2: dictation and enhancement disclosures remain single-hop and keep their existing endpoint derivation semantics.
    - Test 3: unconfigured/invalid providers still render safe "Not configured" or "Invalid endpoint" rows instead of crashing.
  </behavior>
  <action>Extend `PrivacyDisclosureFormatter` so command mode no longer uses placeholder “planned” text. Model command disclosure as the real two-hop flow described in research: spoken instruction audio uploads to the effective STT provider endpoint, then selected text plus the transcribed instruction go to the effective text provider endpoint. Add endpoint-purpose labels to the disclosure model so both settings and IME can render the two hops explicitly, while still using the same shared formatter source. Update `PrivacySafetyScreen` and its disclosure card rendering to show the labeled command hops from live provider config. Reuse existing base-URL/path derivation rules and Gemini normalization; do not duplicate command copy in settings code.</action>
  <verify>
    <automated>./android/gradlew -p android testDebugUnitTest --tests "com.github.shekohex.whisperpp.privacy.PrivacyDisclosureFormatterTest"</automated>
  </verify>
  <done>Command disclosures are truthful, two-hop, and shared between Privacy & Safety settings and the first-use modal without drift.</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: move command disclosure gating to pre-recording without bypassing shared blocks</name>
  <files>android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt, android/app/src/test/java/com/github/shekohex/whisperpp/command/CommandDisclosureFlowTest.kt</files>
  <behavior>
    - Test 1: first-time command mode requests disclosure after text confirmation but before any `recorderManager.start(...)` call.
    - Test 2: canceling or opening Privacy & Safety from that disclosure exits command mode cleanly and does not transcribe or transform anything.
    - Test 3: after consent, the existing `shouldBlockExternalSend()` gate still runs before recording, before STT, and before the text transform call.
  </behavior>
  <action>Refactor only the command listening path in `WhisperInputService` so the command-mode first-use disclosure happens after `commandConfirmedText` is known but before instruction recording begins. Build the disclosure with the effective STT provider/model plus the effective command text provider/model, then call `awaitFirstUseDisclosure(FirstUseDisclosureMode.COMMAND_TEXT, ...)` from the `startCommandListening()` path before `recorderManager.start(recordedAudioFilename)`. Keep the locked single-gate decision intact: continue using `shouldBlockExternalSend()` before command entry, again before recording, again before STT, and again before the LLM transform. Do not move disclosure later into `onCommandStopListening()`, and do not replace the existing mode-specific disclosure flag with a global one.</action>
  <verify>
    <automated>./android/gradlew -p android testDebugUnitTest --tests "com.github.shekohex.whisperpp.command.CommandDisclosureFlowTest" --tests "com.github.shekohex.whisperpp.privacy.PrivacyDisclosureFormatterTest"</automated>
  </verify>
  <done>Command mode cannot start recording spoken instruction audio until command disclosure consent is resolved, and shared secure-field/app-policy blocking still guards every external-send step.</done>
</task>

  <task type="auto">
  <name>task 3: prove Privacy & Safety disclosure/reset/send-policy UI with instrumentation</name>
  <files>android/app/src/androidTest/java/com/github/shekohex/whisperpp/ui/settings/PrivacySafetyScreenUiTest.kt</files>
  <action>Finish the `PrivacySafetyScreenUiTest` harness from plan 09-01 so it verifies the shipped privacy UI after the formatter/runtime changes land. Cover: the command disclosure card renders both labeled hops, the reset-disclosure action clears `DISCLOSURE_SHOWN_DICTATION_AUDIO`, `DISCLOSURE_SHOWN_ENHANCEMENT_TEXT`, and `DISCLOSURE_SHOWN_COMMAND_TEXT`, and per-app policy controls still allow blocking/search/manual package entry. Reuse same-package fixture rendering patterns from `Phase06VerificationUiTest`; avoid introducing brittle text-only selectors when a stable same-package seam is available.</action>
  <verify>
    <automated>./android/gradlew -p android connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.github.shekohex.whisperpp.ui.settings.PrivacySafetyScreenUiTest</automated>
  </verify>
  <done>`PrivacySafetyScreenUiTest` runs its instrumentation assertions and proves the privacy screen’s command disclosure, reset flow, and per-app policy controls.</done>
</task>

</tasks>

<verification>
- `./android/gradlew -p android testDebugUnitTest --tests "com.github.shekohex.whisperpp.privacy.PrivacyDisclosureFormatterTest" --tests "com.github.shekohex.whisperpp.command.CommandDisclosureFlowTest"` passes.
- `./android/gradlew -p android :app:assembleDebug` passes.
- `./android/gradlew -p android connectedDebugAndroidTest -Pandroid.testInstrumentationRunnerArguments.class=com.github.shekohex.whisperpp.ui.settings.PrivacySafetyScreenUiTest` passes with assertions covering the two-hop command disclosure, reset flow, and per-app policy controls.
</verification>

<success_criteria>
- Command disclosure no longer says command mode is planned.
- First-use command consent happens before audio capture, not after transcription.
- Privacy & Safety settings and the IME first-use modal stay in sync because both read from the same disclosure formatter.
</success_criteria>

<output>
After completion, create `.planning/phases/09-privacy-traceability-command-disclosure-alignment/09-02-SUMMARY.md`
</output>
