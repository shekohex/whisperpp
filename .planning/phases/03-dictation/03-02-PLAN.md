---
phase: 03-dictation
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/WhisperTranscriber.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/dictation/DictationController.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/dictation/FocusKey.kt
  - android/app/src/test/java/com/github/shekohex/whisperpp/WhisperTranscriberLanguageTest.kt
autonomous: true
requirements:
  - DICT-01
  - DICT-03
  - DICT-04
  - DICT-05
  - DICT-07

must_haves:
  truths:
    - "If streaming is unsupported/disabled, no partials are inserted; only the final transcript is inserted on send"
    - "Cancel stops dictation and prevents late inserts into any editor"
    - "Changing focus/app/IME hiding pauses dictation and prevents cross-field insertion"
    - "Selected language is applied to STT requests (auto omits/auto-selects)"
  artifacts:
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/dictation/DictationController.kt"
      provides: "Session-tokened dictation orchestration with focus safety gates"
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/dictation/FocusKey.kt"
      provides: "Editor focus-instance identity (fieldId + focusInstanceId) for DICT-07 guards"
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt"
      provides: "IME wiring for dictation state transitions, auto-pause on lifecycle/focus"
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/WhisperTranscriber.kt"
      provides: "Language-aware non-streaming STT request builder"
    - path: "android/app/src/test/java/com/github/shekohex/whisperpp/WhisperTranscriberLanguageTest.kt"
      provides: "Unit coverage for language parameter propagation"
  key_links:
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt"
      to: "android/app/src/main/java/com/github/shekohex/whisperpp/dictation/DictationController.kt"
      via: "UI callbacks + IME lifecycle hooks"
      pattern: "DictationController\(" 
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/dictation/DictationController.kt"
      to: "android/app/src/main/java/com/github/shekohex/whisperpp/WhisperTranscriber.kt"
      via: "Non-streaming finalize-on-send transcription"
      pattern: "startAsync\("
---

<objective>
Implement dictation correctness in the IME: pause-only on mic release (no auto-send), explicit send, cancel with no late inserts, focus/app safety auto-pause, and applying the selected dictation language to STT requests.

Purpose: Deliver DICT-03/04/05/07 foundation so dictation is reliable across arbitrary editors and lifecycle changes.
Output: DictationController + FocusKey, updated IME wiring, language-aware STT request support + tests.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-dictation/03-CONTEXT.md
@.planning/phases/03-dictation/03-RESEARCH.md

@android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/WhisperTranscriber.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FocusKey + DictationController with session token + focus-safety gates</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/dictation/FocusKey.kt
android/app/src/main/java/com/github/shekohex/whisperpp/dictation/DictationController.kt
  </files>
  <action>
- Create `FocusKey` capturing a conservative identity for the current editor focus instance (DICT-07):
  - MUST include `packageName` and `inputType` from `EditorInfo`.
  - Include `fieldId` when available from `EditorInfo` (null/absent on older API levels).
  - Include a `focusInstanceId` that changes on every new editor-focus instance:
    - Maintain a monotonic counter in `WhisperInputService`.
    - Increment the counter in BOTH `onStartInput` and `onStartInputView` (treat each call as a new focus instance).
    - `FocusKey` equality MUST include `focusInstanceId` so late callbacks cannot match a newer focused field.

- Create `DictationController` owned by `WhisperInputService` that:
  - Maintains a monotonic `sessionId` token.
  - Stores `focusKeyAtStart` and current focus key.
  - Exposes explicit commands for UI + system events (start hold, pause, resume, lock, unlock, send, cancel-confirmed, focus-changed, window-hidden/shown).
  - Enforces DICT-07: any callback that can insert text MUST check `sessionId` and current focus safety before inserting.
  - Implements DICT-03 behavior flag: when streaming partials are unsupported/disabled, controller never calls composing insertion APIs and only finalizes on explicit send.
  - Cancellation is terminal for a session: stop recorder, cancel transcriber/network, clear any composing (if present later), and ignore late callbacks.

- Keep controller Android-touching code minimal and injected:
  - Pass in lambdas for toasts, clipboard write, and `InputConnection` access rather than referencing `WhisperInputService` directly inside helper methods.
  </action>
  <verify>./android/gradlew -p android :app:assembleDebug</verify>
  <done>
- Controller exists and compiles, with explicit event methods and a session token guard.
- FocusKey exists and can be computed from `EditorInfo`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewire WhisperInputService dictation flow to pause on release, explicit send, and auto-pause on lifecycle/focus changes</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
  </files>
  <action>
- Replace the current toggle-style dictation behavior with controller-driven behavior per locked decisions:
  - Releasing after hold pauses (does NOT start transcription).
  - Send triggers finalization (non-streaming: stop recording then run transcription; streaming later plan).
  - Trash cancel uses confirmation; swipe-to-cancel stays immediate.
  - Paused sessions do not expire.

- Implement DICT-07 hooks:
- Implement DICT-07 hooks:
   - In `onStartInput` and `onStartInputView`, increment `focusInstanceId`, compute a new `FocusKey` (including `focusInstanceId` + `fieldId` when available), and notify controller.
   - On focus change while holding mic: auto-pause.
   - On `onWindowHidden`: auto-pause + toast; DO NOT cancel/finalize; keep session paused.
   - Ensure `onWindowHidden` no longer calls `whisperTranscriber.stop()` / `recorderManager.stop()` as a blanket action when dictation is active; route through controller.

- Auto-pause visibility rule (forward-compatible with streaming partials):
  - Auto-pause MUST NOT clear composing/partial text if any is visible.
  - Only cancel-confirm clears visible partial/composing immediately.

- Implement clipboard fallback on send when no editable field:
  - If `currentInputConnection == null` or `EditorInfo` indicates no input, copy transcript to clipboard, toast, and keep session paused.

- Keep secure-field + per-app send policy gate in place before any network-bound send.

- Disable manual text edits while recording is active (defense in depth):
  - When controller state is `Recording` or `RecordingLocked`, ignore normal key events/edits from the keyboard surface; allow only dictation controls.
  </action>
  <verify>./android/gradlew -p android :app:assembleDebug</verify>
  <done>
- Mic release pauses only; transcript inserts only on explicit send.
- IME hide/app switch/focus change pauses session and prevents cross-field insertion.
- Cancel stops everything for the session and prevents late inserts.
  </done>
</task>

<task type="auto">
  <name>Task 3: Apply dictation language selection to STT requests + add unit tests</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/WhisperTranscriber.kt
android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
android/app/src/test/java/com/github/shekohex/whisperpp/WhisperTranscriberLanguageTest.kt
  </files>
  <action>
- Thread `LANGUAGE_CODE` into transcription requests:
  - In `WhisperInputService`, load selected `LANGUAGE_CODE` at send time and pass it into the transcriber call.
  - In `WhisperTranscriber`, accept a `languageCode: String` parameter ("auto" allowed).

- Request behavior:
  - For OpenAI/CUSTOM multipart: add `language` form field when `languageCode != "auto"`.
  - For WHISPER_ASR query params: replace the hardcoded `language=auto` with the selected language; keep `auto` when selected.

- Add JUnit4 tests in `WhisperTranscriberLanguageTest.kt` that validate:
  - WHISPER_ASR URL query param uses selected language and defaults to auto.
  - Multipart body for OpenAI/CUSTOM includes language part only when not auto.
  </action>
  <verify>./android/gradlew -p android testDebugUnitTest :app:assembleDebug</verify>
  <done>
- Language selection changes the outbound STT request.
- Unit tests pass for WHISPER_ASR URL and multipart form behavior.
  </done>
</task>

</tasks>

<verification>
- Build: `./android/gradlew -p android :app:assembleDebug`
- Unit tests: `./android/gradlew -p android testDebugUnitTest`
</verification>

<success_criteria>
- DICT-03/04/07: no partial insertion in non-streaming mode, cancel prevents late inserts, focus/lifecycle changes pause safely.
- DICT-05: language is applied consistently to STT requests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dictation/03-02-SUMMARY.md`
</output>
