---
phase: 03-dictation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/keyboard/KeyboardState.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
autonomous: true
requirements:
  - DICT-01

must_haves:
  truths:
    - "Mic interactions feel like hold-to-talk with pause (no auto-send), with a visible recording state"
    - "Locked hands-free mode is reachable via swipe-up while holding"
    - "Paused/locked controls show trash-left, resume-center, send-right"
  artifacts:
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/keyboard/KeyboardState.kt"
      provides: "Dictation-relevant IME UI states (recording/locked/paused/transcribing)"
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt"
      provides: "Gesture + controls UI for dictation (hold, lock, cancel swipe, paused controls)"
  key_links:
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt"
      to: "android/app/src/main/java/com/github/shekohex/whisperpp/keyboard/KeyboardState.kt"
      via: "State-driven composables + gesture handlers"
      pattern: "when \(targetState\)"
---

<objective>
Update the IME dictation bar UI to match the locked WhatsApp/Telegram voice-note flow: hold-to-talk with pause on release, swipe-up to lock, longer swipe-to-cancel, and paused/locked controls (trash/resume/send) with clear recording/paused visuals.

Purpose: Make Phase 3 dictation behavior understandable and controllable from the keyboard surface.
Output: Updated KeyboardState + KeyboardScreen gesture/controls UI.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-dictation/03-CONTEXT.md
@.planning/phases/03-dictation/03-RESEARCH.md

@android/app/src/main/java/com/github/shekohex/whisperpp/keyboard/KeyboardState.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend KeyboardState for paused+locked control surface</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/keyboard/KeyboardState.kt
  </files>
  <action>
- Add a state that can represent "paused while in locked hands-free mode" so the UI can render a lock affordance while paused.
- Keep existing states; do not rename existing enum values.
- Ensure the UI can still represent: Ready, Recording, RecordingLocked, Paused, Transcribing, SmartFixing.
  </action>
  <verify>./android/gradlew -p android :app:assembleDebug</verify>
  <done>
- `KeyboardState` can represent paused/locked distinctly (for UI rendering only; service wiring comes later).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update KeyboardScreen gestures + paused/locked controls to match Phase 3 decisions</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/ui/keyboard/KeyboardScreen.kt
  </files>
  <action>
- Preserve the overall look, but adjust behavior per locked decisions in `03-CONTEXT.md`:
  - Hold-to-talk remains the primary gesture.
  - Releasing after hold MUST result in "paused" (no auto-send).
  - Swipe-up while holding enters locked hands-free mode.
  - Swipe-to-cancel remains available in both hold and locked; increase swipe-to-cancel threshold vs today to reduce accidental cancels.
  - In paused/locked controls, render three explicit actions: trash (left), resume (center), send (right).
  - Recording state uses the existing waveform; paused state shows paused icon + timer (keep timer visible while paused).
  - Haptics remain on start/lock/cancel transitions.

- Disable manual text edits while recording is active:
  - When dictation state is `Recording` or `RecordingLocked`, disable/ignore normal typing + edit keys (letters, space, enter, backspace, cursor moves, selection changes).
  - Keep dictation controls functional (mic hold/release, lock/unlock, resume, send, cancel/discard).
  - When paused (not recording), normal typing/edit keys remain enabled.

- Implementation notes:
  - Use existing callbacks where possible (`onMicAction`, `onCancelAction`, `onSendAction`, `onLockAction`, `onUnlockAction`, `onDiscardAction`).
  - Trash button must call the confirmation-based path (`onCancelAction`); swipe-to-cancel should remain the immediate path (`onDiscardAction`).
  - Do not introduce new settings/toggles in this plan.
  </action>
  <verify>./android/gradlew -p android :app:assembleDebug</verify>
  <done>
- UI shows a clear recording state during hold/locked.
- Paused/locked surface shows trash-left, resume-center, send-right.
- Swipe-to-cancel requires a longer swipe than before.
- While recording/locked, normal typing/edit keys are disabled; only dictation controls respond.
  </done>
</task>

</tasks>

<verification>
- Build: `./android/gradlew -p android :app:assembleDebug`
</verification>

<success_criteria>
- Dictation UI matches the locked decisions (hold-to-talk, lock swipe-up, longer cancel swipe, paused controls).
</success_criteria>

<output>
After completion, create `.planning/phases/03-dictation/03-01-SUMMARY.md`
</output>
