---
phase: 03-dictation
plan: 04
type: execute
wave: 4
depends_on:
  - "03-03"
files_modified:
  - android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/dictation/DictationController.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/dictation/OpenAiRealtimeSttClient.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/recorder/RecorderManager.kt
  - android/app/src/main/java/com/github/shekohex/whisperpp/recorder/RiffWaveHelper.kt
autonomous: true
requirements:
  - DICT-02

must_haves:
  truths:
    - "When the selected STT model supports streaming, partial transcription appears as composing text while speaking"
    - "Partial updates are throttled/coalesced (no jittery per-chunk updates)"
    - "Finalization atomically replaces composing content and always runs the replace path"
    - "If streaming fails mid-session, dictation keeps visible partial text and can still finalize best-effort"
    - "If streaming is flagged but the provider/protocol is unsupported, dictation downgrades to non-streaming (toast) and inserts no partials"
  artifacts:
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/dictation/OpenAiRealtimeSttClient.kt"
      provides: "OkHttp WebSocket client for OpenAI Realtime transcription events"
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/dictation/DictationController.kt"
      provides: "Composing-text lifecycle (setComposingText throttle, finishComposingText on finalize)"
    - path: "android/app/src/main/java/com/github/shekohex/whisperpp/recorder/RecorderManager.kt"
      provides: "PCM chunk callback support for streaming audio"
  key_links:
    - from: "android/app/src/main/java/com/github/shekohex/whisperpp/recorder/RecorderManager.kt"
      to: "android/app/src/main/java/com/github/shekohex/whisperpp/dictation/OpenAiRealtimeSttClient.kt"
      via: "PCM chunk flow -> input_audio_buffer.append"
      pattern: "append"
---

<objective>
Add streaming dictation partials: when the selected STT model indicates streaming support, stream audio while recording and insert partial transcripts inline as composing text (throttled), then finalize atomically on send.

Purpose: Deliver DICT-02 without regressing focus safety, cancellation, or undo.
Output: OpenAI Realtime streaming client + recorder chunking + composing insertion lifecycle.
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-dictation/03-CONTEXT.md
@.planning/phases/03-dictation/03-RESEARCH.md

@android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
@android/app/src/main/java/com/github/shekohex/whisperpp/recorder/RecorderManager.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PCM chunk callback support in RecorderManager (streaming-friendly)</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/recorder/RecorderManager.kt
android/app/src/main/java/com/github/shekohex/whisperpp/recorder/RiffWaveHelper.kt
  </files>
  <action>
- Extend `RecorderManager.start(...)` so dictation can optionally receive raw PCM chunks while recording, without blocking file recording.
- Keep existing API working for non-streaming dictation.

- If you change sample rate for streaming sessions:
  - Update `RiffWaveHelper.headerBytes(...)` so WAV headers reflect the actual sample rate used.
  - Keep non-streaming default behavior unchanged.
  </action>
  <verify>./android/gradlew -p android :app:assembleDebug</verify>
  <done>
- Recorder can emit PCM chunks during recording while still writing a valid WAV file (if used for fallback).
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement OpenAI Realtime streaming STT client (OkHttp WebSocket) gated by model capability</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/dictation/OpenAiRealtimeSttClient.kt
android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
  </files>
  <action>
- Implement `OpenAiRealtimeSttClient` using OkHttp WebSocket:
  - Build the realtime URL from the provider base URL (e.g. `/v1/realtime?model=<modelId>`), upgrading scheme to `wss`.
  - Authenticate with the provider API key (reuse existing key injection rules; do not log keys).
  - Send audio chunks via the realtime event model (append + commit) and parse transcription delta/completed events.

- In `WhisperInputService`/controller wiring:
 - In `WhisperInputService`/controller wiring:
   - Only enable streaming when the selected STT `ModelConfig.streamingPartialsSupported == true` AND the provider is OpenAI Realtime (protocol supported).
   - If the model is flagged for streaming but the provider/protocol is unsupported:
     - Downgrade to non-streaming for this session (record to file + finalize on send).
     - Show a short toast explaining streaming partials aren't supported for this provider/model.
     - Ensure no partial/composing insertion is attempted in the downgraded path.
   - If streaming connection fails to establish or drops:
     - Keep any currently visible composing/partial text.
     - Continue session in best-effort mode and allow finalize on send.
   - Ensure cancellation closes the WebSocket and prevents late events from inserting.
  </action>
  <verify>./android/gradlew -p android :app:assembleDebug</verify>
  <done>
- Streaming client can connect, stream audio, and produce partial+final transcript callbacks.
- Streaming is capability-gated by the selected STT model.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add composing-text insertion lifecycle with throttled partial updates and atomic finalize-on-send</name>
  <files>
android/app/src/main/java/com/github/shekohex/whisperpp/dictation/DictationController.kt
android/app/src/main/java/com/github/shekohex/whisperpp/WhisperInputService.kt
  </files>
  <action>
- Implement composing lifecycle for streaming partials:
 - Implement composing lifecycle for streaming partials:
   - Partial updates use `InputConnection.beginBatchEdit` + `setComposingText(partial, 1)`.
   - Throttle/coalesce partial updates (time-based and/or suppress identical consecutive partials).
   - Finalization on send ALWAYS runs the replace path:
     - `setComposingText(final)` then `finishComposingText()` inside a batch edit.
   - Pause/auto-pause MUST preserve any visible composing/partial text.
   - Cancel-confirmed clears composing immediately and ensures no further insertions for the session.

- Fallback behavior:
 - Fallback behavior:
   - If the streaming connection fails/drops mid-session, keep the last visible composing text and allow best-effort finalization on send.
  </action>
  <verify>./android/gradlew -p android :app:assembleDebug</verify>
  <done>
- While speaking, partial transcripts appear as composing text when streaming is enabled.
- On send, composing text is atomically replaced with final transcript and composing is finished.
  </done>
</task>

</tasks>

<verification>
- Build: `./android/gradlew -p android :app:assembleDebug`
</verification>

<success_criteria>
- DICT-02: streaming partials insert as composing text while speaking (capability-gated).
- Partial updates are throttled and finalization is atomic.
</success_criteria>

<output>
After completion, create `.planning/phases/03-dictation/03-04-SUMMARY.md`
</output>
